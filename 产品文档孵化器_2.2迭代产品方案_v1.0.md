# 产品文档孵化器 2.2 迭代产品方案

## 版本信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编制日期 | 2026 年 8 月 17 日 |
| 产品版本 | 产品文档孵化器 2.2 |
| 前置版本 | 产品文档孵化器 2.1 |
| 目标用户 | Owner（产品经理） |
| 迭代主题 | 完整 Wiki-LLM 项目脚手架与标准 Ingest 增量知识维护 |
| 参考理念 | [Karpathy LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) |
| 推荐开发投入 | 预计 9.5 人日，封顶 12 人日 |

> 本文档是产品文档孵化器 2.2 的产品、设计、开发范围和验收基线。2.2 的目标不是增加一个同名按钮，而是让“归档、Ingest、文档孵化、发布”成为语义清晰且持续积累的完整 Wiki-LLM 工作流。

---

## 一、需求背景

### 1.1 当前问题

2.0 和 2.1 已建立 `raw/`、`wiki/`、`schema/` 三层目录，也已生成 `wiki/index.md`、`wiki/log.md` 和 `schema/AGENTS.md`。但实际体验仍与 Wiki-LLM 理念存在四个差距：

1. 项目根目录没有 `README.md`，Owner 用 Finder、Obsidian 或编辑器打开项目时，不知道项目用途、目录职责和下一步操作；
2. Agent 规则只位于 `schema/AGENTS.md`，从项目根目录进入的 Codex 不一定自动发现和采用该规则；
3. 当前“文档孵化”主要从原始材料直接生成一份候选产品方案，没有为每份来源建立持久总结页，也没有持续更新主题页和内容索引；
4. “Ingest”在部分文档中被当作“导入文件”或“生成产品方案”的近义词，未严格采用 Wiki-LLM 中“将新来源编译进持久 Wiki”的含义。

这会导致系统虽然拥有类似的目录名称，但知识仍然在每次生成产品文档时被重新理解，没有形成持续积累、可浏览、可追溯的 Wiki 中间层。

### 1.2 Wiki-LLM 的标准含义

2.2 统一采用以下定义：

- **Raw sources**：Owner 归档的不可变原始材料，是事实来源；
- **Wiki**：AI 持续生成和维护的 Markdown 知识层，包括来源总结、主题页、索引和日志；
- **Schema**：告诉 AI 如何 Ingest、维护引用、更新主题和检查 Wiki 的规则；
- **Ingest**：读取一份新来源，把其有效信息增量编译进已有 Wiki，并更新索引、主题、矛盾和日志；
- **Document Incubation**：基于已经维护好的 Wiki 生成或修订完整产品方案候选；
- **Publish**：Owner 批准候选后，使其成为唯一当前生效产品方案。

Ingest 不等于上传，不等于归档，也不等于发布产品方案。

## 二、迭代目标

2.2 必须实现：

1. 新建项目自动生成完整、可直接用 Obsidian 浏览的 Wiki-LLM 项目结构；
2. 项目根目录自动生成 `README.md` 和可被 Agent 自动识别的根目录 `AGENTS.md`；
3. 新建项目自动生成 `wiki/sources/`，用于保存每份已 Ingest 来源的总结页；
4. Schema 中提供唯一、明确的 Ingest 契约，UI、应用服务、Agent 指令和产品文档使用同一含义；
5. 材料归档后进入“待 Ingest”，不得直接标记为已完成知识处理；
6. Owner 主动发起单材料 Ingest；系统读取来源、生成来源总结、更新主题页、更新 `wiki/index.md`、追加 `wiki/log.md`；
7. 每次成功 Ingest 都形成可验证、可追溯、原子提交的 Wiki 变更；
8. 产品文档孵化优先使用已 Ingest 的 Wiki 成果，不再把直接读取 Raw 作为默认路径；
9. 当前产品方案仍采用“候选—Owner 审核—发布”的治理流程，Ingest 不得自动发布；
10. 保留 2.1 的材料类型、版本链、两级权威、安全等级和 L3/L4 本地处理规则；
11. 不批量改写现有项目；2.2 新脚手架首先应用于此后新建的项目。

### 2.1 成功标准

- Owner 新建项目后，在项目根目录可以直接看到 `README.md` 和 `AGENTS.md`；
- 用 Obsidian 打开项目根目录后，能够从 README 进入 Wiki 索引、来源总结和主题页面；
- 归档一份材料后，系统明确显示“待 Ingest”；
- 完成一次 Ingest 后，至少新增或更新一份来源总结页、内容索引和操作日志；
- 材料涉及已有主题时，相应主题页被增量更新，而不是另建互不关联的重复页面；
- 新来源与旧知识冲突时，系统保留双方来源并显式记录冲突，不静默覆盖；
- 产品方案候选可以追溯到 Wiki 页面，Wiki 页面可以追溯到 Raw 来源 ID；
- 同一来源、同一内容和同一 Schema 版本重复执行时，不产生重复页面或重复有效内容；
- Ingest 任一步失败时，现有 Wiki 保持原状，材料保留为可重试状态；
- 任一项目的 Ingest 不读取或修改其他项目的 Raw、Wiki、Schema 和日志。

## 三、方案比较与选择

### 3.1 方案 A：完整 Wiki 增量管线（选定）

```text
归档 Raw
→ Owner 发起 Ingest
→ 更新来源总结、主题、索引、矛盾和日志
→ 基于 Wiki 孵化产品文档候选
→ Owner 审核并发布
```

优点：与 Wiki-LLM 的核心含义一致；知识可持续积累；Obsidian 可直接浏览；后续 Query、Lint 和产品方案生成均可复用 Wiki。

代价：需要新增 Wiki 变更集、原子写入和 Ingest 状态治理，投入高于只增加目录文件。

### 3.2 方案 B：只补 README，继续直接生成产品方案（不采用）

优点：投入最低，现有流程改动少。

缺点：仅改善目录观感，Ingest 仍没有形成来源总结、主题页和持续知识积累，不满足本次目标。

### 3.3 方案 C：归档成功后自动 Ingest（不采用）

优点：操作步骤少。

缺点：Owner 无法在调用模型前复核材料属性、脱敏和外部授权；批量上传时成本和失败边界不清晰；不符合单材料、Owner 参与的轻量治理原则。

2.2 采用方案 A，并保留“确认归档”和“开始 Ingest”两个明确动作。

## 四、核心概念与统一术语

| 术语 | 唯一定义 | 不等同于 |
| --- | --- | --- |
| 选择文件 | 文件暂存于本次页面会话，尚未写入项目 | 归档、Ingest |
| 确认归档 | 将原件副本写入 `raw/`，保存材料元数据与 SHA-256 | Ingest、产品方案生成 |
| Ingest | 把一份已归档来源增量编译进持久 Wiki | 上传、归档、发布 |
| 来源总结页 | AI 或本地人工依据单一 Raw 来源生成的结构化 Markdown | 原文件副本 |
| 主题页 | 综合多份来源、持续维护的概念或业务主题 Markdown | 单一来源摘要 |
| Wiki 变更集 | 一次 Ingest 准备新增、更新的全部 Markdown 和索引操作 | 当前生效产品方案 |
| 产品文档孵化 | 根据当前 Wiki 生成完整产品方案候选 | Ingest |
| 发布 | Owner 使候选成为当前唯一生效产品方案 | 保存候选 |

所有界面、README、AGENTS、Schema、日志、状态字段和验收用例都必须使用上述定义。

## 五、新项目自动生成结构

### 5.1 目录结构

```text
{项目ID}/
├── README.md
├── AGENTS.md
├── raw/
│   └── {年份}/{来源ID}/原始文件
├── wiki/
│   ├── index.md
│   ├── log.md
│   ├── sources/
│   │   └── {来源ID}-{材料名称}.md
│   ├── topics/
│   │   └── {主题名称}.md
│   ├── drafts/
│   ├── current/
│   └── versions/
├── schema/
│   ├── AGENTS.md
│   ├── ingest-contract.md
│   ├── source-page-template.md
│   ├── topic-page-template.md
│   ├── product-document-template.md
│   └── field-conventions.md
├── exports/
└── .incubator/
    ├── project.json
    ├── source-index.json
    └── current-baseline.json（首次发布后生成）
```

### 5.2 根目录 README.md

新建项目时根据项目名称、ID、说明和创建时间生成，至少包含：

1. 项目定位和 Owner；
2. 当前状态和下一步操作；
3. Raw、Wiki、Schema、Exports、`.incubator` 的职责；
4. `归档 → Ingest → 孵化 → 发布` 的标准流程；
5. 指向 `wiki/index.md`、`wiki/log.md` 和 Schema 的相对链接；
6. 安全说明：Raw 不可修改、L3/L4 不得外发、当前方案只能由发布流程写入；
7. Obsidian 使用提示：将项目根目录作为 Vault 打开，从 README 或 `wiki/index.md` 开始浏览。

README 是人工入口，不保存动态业务正文，不需要在每次 Ingest 时重写。

### 5.3 根目录 AGENTS.md

根目录 `AGENTS.md` 是 Agent 自动发现的操作入口，至少要求 Agent：

- 首先阅读 `schema/ingest-contract.md` 和 `wiki/index.md`；
- Raw 只读，不得修改、移动、重命名或删除；
- Ingest 必须以单一 `source_id` 为输入；
- 更新来源页、相关主题页、索引和日志必须作为同一变更集；
- 每个事实或结论必须保留来源 ID 和定位信息；
- 遇到冲突必须并列记录，不得无依据选择其中一方；
- 不得把候选知识直接写入 `wiki/current/`；
- 不得跨项目读取正文；
- L3/L4 不得调用外部模型。

`schema/AGENTS.md` 保留为兼容入口，但只说明“项目根 `AGENTS.md` 为操作规则的唯一基线”，避免维护两套相互漂移的规则。

### 5.4 Schema 文件

| 文件 | 用途 |
| --- | --- |
| `ingest-contract.md` | 定义 Ingest 的输入、步骤、输出、失败与幂等规则 |
| `source-page-template.md` | 约束来源总结页的 Frontmatter、章节和引用格式 |
| `topic-page-template.md` | 约束主题页的综合结论、来源、冲突和待确认项 |
| `product-document-template.md` | 约束正式产品方案候选的结构 |
| `field-conventions.md` | 约束 ID、版本、编码、链接和字段口径 |

## 六、标准 Ingest 契约

### 6.1 输入条件

一次 Ingest 只处理一份已归档材料。开始前必须满足：

- 来源属于当前项目；
- Raw 文件存在且 SHA-256 与归档记录一致；
- 材料类型、材料版本、权威级别和安全等级有效；
- 来源未处于另一个 Ingest 处理中；
- 外部模型路径同时满足项目授权、材料授权、L1/L2 和已脱敏；
- L3/L4 只能进入本地人工 Ingest 路径。

### 6.2 标准步骤

```text
读取 wiki/index.md 与 ingest-contract.md
→ 校验 Raw 完整性和材料权限
→ 读取本次单一来源
→ 生成或更新 wiki/sources/{来源页}.md
→ 根据内容查找已有 wiki/topics 页面
→ 更新相关主题页或创建必要的新主题页
→ 标记新增事实、被强化结论、冲突、失效信息和证据缺口
→ 更新 wiki/index.md
→ 追加 wiki/log.md
→ 校验所有 Wiki 链接、引用和项目边界
→ 原子提交整个 Wiki 变更集
→ 来源状态变为“已 Ingest”
```

不得跳过来源总结页、索引或日志，只生成产品方案候选。

### 6.3 来源总结页

每份成功 Ingest 的材料对应一份来源总结页。页面至少包含：

```yaml
---
source_id: SRC-XXXX
material_name: 示例材料
material_version: v1.0
source_type: product_requirement
authority_level: formal_effective
security_level: L2
raw_sha256: 完整哈希
ingested_at: 2026-08-17T00:00:00Z
ingest_schema_version: "2.2"
---
```

正文至少包含：

- 来源摘要；
- 关键事实与规则；
- 对当前 Wiki 的新增或修订；
- 相关主题链接；
- 与既有知识的冲突；
- 证据缺口与待 Owner 确认事项；
- 引用定位。

来源页只保存结构化总结和必要短摘录，不复制整个 Raw 文件。

### 6.4 主题页

主题页面向跨来源综合，至少包含：

- 当前综合结论；
- 正式基线依据；
- 参考材料；
- 已知冲突；
- 待确认问题；
- 关联来源和其他主题的 Obsidian 双向链接。

正式基线依据和参考材料必须分区表达。参考材料不得被自动改写为已生效规则。

### 6.5 index.md

`wiki/index.md` 是内容导航，不是操作流水。每次 Ingest 后必须更新：

- 项目简介和当前生效产品方案链接；
- 来源总结页目录，每项含链接、材料名称、版本和一句话摘要；
- 主题页目录，每项含链接和一句话说明；
- 已知冲突和证据缺口入口；
- 最近一次成功 Ingest 的来源和时间。

### 6.6 log.md

`wiki/log.md` 是只追加时间线，不得改写历史记录。统一格式：

```text
## [2026-08-17 09:30] ingest | SRC-XXXX | 材料名称

- 结果：成功
- 新增：[[sources/SRC-XXXX-材料名称]]
- 更新：[[topics/准入规则]]、[[index]]
- 冲突：1 项
- 执行方式：外部模型 / 本地人工
```

失败记录只写错误码和受影响文件名，不写敏感正文。

### 6.7 幂等与重新 Ingest

- 幂等键由 `project_id + source_id + raw_sha256 + ingest_schema_version` 组成；
- 相同幂等键已经成功时，再次执行返回已有结果，不追加重复内容和成功日志；
- Raw 内容变化必须先被完整性校验阻断，不允许静默重新 Ingest；
- Schema 版本升级后，Owner 可以明确选择“重新 Ingest”；
- 同一材料的新版本是新的来源记录，应按新来源 Ingest，同时在来源页标明前一版本；
- 重新 Ingest 仍必须生成一个完整变更集并保留审计记录。

## 七、产品文档孵化与 Ingest 的关系

### 7.1 数据来源优先级

产品文档孵化默认只使用：

1. `wiki/index.md`；
2. 与本次目标相关的 `wiki/topics/` 页面；
3. 对应的 `wiki/sources/` 来源总结页；
4. 当前生效产品方案；
5. 产品文档 Schema。

只有在引用定位复核时，才读取相应 Raw 原件。不得每次孵化都重新把全部 Raw 当作无结构材料交给模型。

### 7.2 候选与发布

```text
选择已 Ingest 来源或主题
→ 读取持久 Wiki
→ 生成产品方案候选
→ 展示 Wiki 覆盖、来源、冲突和证据缺口
→ Owner 编辑
→ 查看当前方案与候选 Diff
→ Owner 批准
→ 发布为唯一当前产品方案
```

Ingest 只更新知识层；产品方案候选不会因 Ingest 自动发布。

### 7.3 未完成 Ingest 的材料

- “待 Ingest”“Ingest 失败”的材料默认不能参与外部产品文档孵化；
- Owner 可查看 Raw 和错误信息并重新执行 Ingest；
- L3/L4 可通过本地人工 Ingest 形成来源页和主题更新，再参与本地候选修订；
- 不允许为了绕过 Ingest 失败而直接把原文发送给产品文档生成工作流。

## 八、材料与 Ingest 状态

```text
已归档（待 Ingest）
→ Ingest 中
→ 已 Ingest
```

异常分支：

```text
Ingest 中 → Ingest 失败 → Owner 重试 → Ingest 中
已 Ingest → Schema 升级 → 建议重新 Ingest
```

| 状态 | 含义 | 可执行操作 |
| --- | --- | --- |
| `pending_ingest` | Raw 已安全归档，尚未进入 Wiki | 开始 Ingest |
| `ingesting` | 正在生成并校验 Wiki 变更集 | 查看进度，不可重复提交 |
| `ingested` | 来源已成功编译进当前 Wiki | 查看 Wiki 结果、参与孵化 |
| `ingest_failed` | 本次变更未提交，现有 Wiki 未变化 | 查看错误、重新 Ingest |
| `reingest_recommended` | Schema 已升级，旧结果仍可用但建议更新 | 明确重新 Ingest |
| `local_review_required` | L3/L4 等材料需要 Owner 本地人工维护 | 本地对照、编辑来源页和主题更新 |

## 九、Owner 使用流程

### 9.1 新建项目

```text
项目中心填写项目信息
→ 系统在临时目录生成完整 Wiki-LLM 脚手架
→ 校验 README、AGENTS、Schema、Wiki 和系统文件
→ 原子提交项目目录
→ 项目显示“Wiki 已初始化”
→ Owner 可复制路径并用 Obsidian 打开
```

### 9.2 归档并 Ingest

```text
选择单份材料
→ 确认材料身份、版本、分类、权威和安全属性
→ 确认归档
→ 页面显示 Raw 路径和“待 Ingest”
→ Owner 点击“开始 Ingest”
→ 系统预检权限与完整性
→ 原子更新来源页、主题页、index 和 log
→ 页面显示本次新增、更新、冲突和证据缺口
```

### 9.3 孵化产品方案

```text
进入文档孵化
→ 选择已 Ingest 的来源或主题
→ 基于 Wiki 生成候选产品方案
→ 查看引用、冲突、缺失项和 Diff
→ Owner 编辑并提交审核
→ Owner 确认发布
```

## 十、界面需求

### 10.1 项目中心

创建成功提示增加：

- `Wiki-LLM 项目已初始化`；
- 本地项目绝对路径；
- `README.md` 和 `wiki/index.md` 的入口说明；
- “用 Obsidian 打开此目录”的操作提示，不自动安装或控制 Obsidian。

项目卡片增加轻量状态：

- Wiki 已初始化 / 初始化异常；
- 待 Ingest 材料数量；
- 已 Ingest 材料数量。

### 10.2 原始材料

材料归档成功后，不再使用模糊的“处理完成”，统一显示：

```text
材料已归档，尚未 Ingest
```

材料卡片显示：

- 材料名称、版本、类型、权威、安全等级；
- Raw 归档路径和 SHA-256；
- Ingest 状态；
- “开始 Ingest”“查看 Wiki 结果”“重新 Ingest”中的合法操作；
- 本次 Ingest 新增页、更新页、冲突和证据缺口数量。

### 10.3 文档孵化

- 默认材料选择列表只展示 `ingested` 来源；
- 每项显示来源总结页和相关主题数；
- 候选上下文显示“本次基于哪些 Wiki 页面生成”；
- 如存在未解决冲突，候选页面必须醒目提示；
- 不得继续使用“已归档材料即可直接参与外部生成”的提示。

### 10.4 当前产品

保持单 Markdown 导出，不增加 Wiki 打包下载。页面可以显示：

- 当前方案关联的 Wiki 页面数量；
- 当前方案仍存在的冲突和证据缺口；
- 指向本地 `wiki/index.md` 的路径。

## 十一、安全与治理规则

1. Raw 永久只读；任何 Ingest 和孵化流程都不得修改 Raw；
2. 外部模型只处理已确认脱敏、允许外部调用的 L1/L2 材料；
3. L3/L4 不得通过摘要、编码、关键词或其他转换外发；
4. L3/L4 的来源页和主题更新由 Owner 在本地人工完成，并标记 `generation_mode: local_manual`；
5. Wiki 页面不得保存 API Key、本地秘密配置或其他项目正文；
6. 正式基线依据和参考材料必须在来源页和主题页中明确区分；
7. 来源冲突不得通过模型自行判定生效方，必须保留双方引用并交给 Owner；
8. Ingest 不得写入 `wiki/current/`、`wiki/versions/` 或发布 Manifest；
9. 项目根路径、来源路径和 Wiki 写入目标必须通过项目边界校验；
10. 错误日志不得包含敏感正文、完整摘录或外部调用密钥。

## 十二、原子性与错误处理

一次 Ingest 的全部文件操作必须先写入项目内临时变更集，验证通过后再提交。以下任一失败，均不得留下部分更新：

- Raw 缺失或哈希不一致；
- Schema 缺失或格式错误；
- 模型超时或返回结构非法；
- 来源页缺少必要元数据；
- 主题页引用其他项目；
- index 出现无效链接；
- log 无法追加；
- SQLite 状态与磁盘结果无法同时提交；
- 提交期间检测到 Wiki 已被其他 Ingest 修改。

失败处理：

1. 丢弃本次临时变更集；
2. 保持提交前 Wiki 字节不变；
3. 来源状态改为 `ingest_failed`；
4. 记录固定错误码和安全摘要；
5. Owner 可以在问题修复后重试。

## 十三、数据与文件要求

### 13.1 项目配置

`.incubator/project.json` 的 Schema 版本升级为 `2.2`，增加：

- `wiki_initialized: true`；
- `wiki_schema_version: "2.2"`；
- `root_readme_path: "README.md"`；
- `root_agent_rules_path: "AGENTS.md"`；
- `ingest_contract_path: "schema/ingest-contract.md"`。

### 13.2 来源索引

每份来源增加或规范：

- `ingest_status`；
- `ingest_schema_version`；
- `ingested_at`；
- `source_page_path`；
- `topic_page_paths`；
- `ingest_result_digest`；
- `ingest_error_code`；
- `generation_mode`。

SQLite 与 `.incubator/source-index.json` 必须保持一致。

### 13.3 Wiki 链接

- 内部链接优先使用 Obsidian 兼容的 `[[相对路径或页面名]]`；
- 来源引用必须同时保留稳定 `source_id`；
- 文件名必须经过安全清洗，不得接受绝对路径或 `..`；
- 页面重命名不属于 2.2，避免引入大规模反向链接维护。

## 十四、兼容与迁移

### 14.1 新项目

2.2 上线后新建项目必须使用完整 2.2 脚手架。缺少任一必需目录或文件时，项目创建整体失败，不登记半成品项目。

### 14.2 已有项目

本次不自动改写 2.0/2.1 已有项目，不覆盖其 README、Schema 或 Wiki 内容。已有项目继续按原流程可用。

后续如增加“升级 Wiki 结构”，必须遵循：只补缺失、先预览、Owner 确认、完整备份、失败回滚；该能力不进入 2.2 本次范围。

### 14.3 既有材料

- 既有材料的 Raw、SHA-256、材料类型、版本关系和权威级别不改变；
- 未有来源总结页的既有材料仍显示原状态；
- 不自动批量 Ingest 历史材料；
- 新项目不存在历史兼容负担，必须完整执行 2.2 标准。

## 十五、本次范围

### 15.1 包含

- 新项目根 README 和根 AGENTS；
- `wiki/sources/` 及来源总结页；
- Ingest 契约、来源页模板、主题页模板；
- 单材料、Owner 发起的标准 Ingest；
- 来源页、主题页、index、log 的原子变更集；
- Ingest 状态、幂等、重试和项目隔离；
- 产品文档孵化改为优先使用 Wiki；
- Obsidian 兼容链接；
- L1/L2 外部模型路径和 L3/L4 本地人工路径；
- 新项目脚手架及自动化验收。

### 15.2 不包含

- 自动修改已有项目目录；
- 批量历史材料 Ingest；
- 文件夹监听和无人值守自动 Ingest；
- 多文件并发 Ingest；
- 自动安装、启动或配置 Obsidian；
- Obsidian 插件开发；
- 向量数据库、qmd、MCP 搜索服务和复杂 RAG；
- 自动合并冲突结论；
- AI 自动发布当前产品方案；
- Wiki ZIP、PDF 或 DOCX 导出；
- Wiki 页面删除、重命名和复杂反向链接迁移；
- 多 Owner、协同编辑和审批流。

## 十六、验收标准

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| AC-01 | 根目录入口 | 新项目自动生成非空 `README.md` 和 `AGENTS.md` |
| AC-02 | 完整脚手架 | 新项目包含 Raw、Wiki、Schema、Exports、系统目录及本文规定的必需文件 |
| AC-03 | README 内容 | README 包含项目身份、目录说明、标准流程、Wiki 链接和安全边界 |
| AC-04 | Agent 自动发现 | 从项目根运行 Agent 时，可根据根 AGENTS 找到 Ingest 契约和 Wiki 索引 |
| AC-05 | 原子创建 | 任一模板缺失或写入失败时，不留下可见项目目录和数据库记录 |
| AC-06 | 归档语义 | 确认归档只写 Raw 和材料元数据，状态为 `pending_ingest`，不伪装成 Ingest 成功 |
| AC-07 | 单来源 Ingest | 一次请求只接受当前项目的一份来源 |
| AC-08 | 来源总结 | 成功 Ingest 后产生结构完整、可追溯 Raw 的来源总结页 |
| AC-09 | 主题维护 | 新来源涉及已有主题时更新原主题页；无适合主题时才创建新页 |
| AC-10 | 索引维护 | index 新增或更新来源、主题、冲突和最近 Ingest 信息，所有链接有效 |
| AC-11 | 日志维护 | log 按统一前缀追加一条成功记录，不改写已有记录 |
| AC-12 | 冲突处理 | 新旧来源矛盾时并列保留双方引用并标记待 Owner 判断 |
| AC-13 | 幂等 | 相同幂等键重复执行不新增重复页、重复段落或重复成功日志 |
| AC-14 | 原子失败 | 任一 Wiki 文件或数据库写入失败时，现有 Wiki 保持提交前状态 |
| AC-15 | 状态治理 | 页面准确显示待处理、处理中、成功、失败、建议重做和本地人工状态 |
| AC-16 | 孵化来源 | 外部产品文档孵化默认只允许选择已 Ingest 来源，并记录使用的 Wiki 页面 |
| AC-17 | Raw 完整性 | Ingest 前后 Raw 文件字节和 SHA-256 完全不变 |
| AC-18 | 安全限制 | L3/L4 Ingest 与孵化均无外部模型调用，错误日志无敏感正文 |
| AC-19 | 项目隔离 | A 项目 Ingest 无法读取、链接或修改 B 项目的任何文件与记录 |
| AC-20 | 现有项目兼容 | 升级应用后，2.0/2.1 已有项目未被自动写入或覆盖，仍可正常打开 |
| AC-21 | Obsidian 浏览 | 将新项目根目录作为 Vault 打开后，可从 README 进入 index、来源页和主题页 |
| AC-22 | 发布边界 | Ingest 成功不得修改当前产品方案、历史版本或发布 Manifest |

## 十七、建议开发拆分与投入

| 工作包 | 内容 | 预计人日 |
| --- | --- | ---: |
| 2.2-A | README、根 AGENTS、Schema 模板及新目录脚手架 | 1.5 |
| 2.2-B | Ingest 状态、来源页模型和来源索引扩展 | 1.5 |
| 2.2-C | Wiki 变更集、原子提交、幂等与回滚 | 2.0 |
| 2.2-D | 单来源 Ingest 工作流、主题/index/log 更新 | 2.0 |
| 2.2-E | 原始材料与文档孵化页面调整 | 1.0 |
| 2.2-F | 安全、隔离、兼容与端到端验收 | 1.5 |
| 合计 |  | 9.5 |

保留最多 2.5 人日风险缓冲，整体不得超过 12 人日。若实现评估超过 12 人日，优先延后项目卡片统计和“建议重新 Ingest”提示，不得裁减根 README/AGENTS、标准 Ingest 语义、来源页、index、log、原子性、安全和项目隔离。

## 十八、最终产品流程

```text
创建产品项目
→ 自动建立 README、AGENTS、Raw、Wiki、Schema
→ Owner 归档单份原始材料
→ 材料进入待 Ingest
→ Owner 发起标准 Ingest
→ 生成来源总结并更新主题、索引、冲突和日志
→ 持久 Wiki 随每份来源持续积累
→ 基于 Wiki 孵化完整产品方案候选
→ Owner 查看引用、缺口和 Diff
→ Owner 批准并发布当前产品方案
→ 后续新来源继续增量 Ingest
```

2.2 的最终判断标准是：系统不再每次从散落的 Raw 材料重新理解项目，而是先把知识持续编译进可浏览、可追溯、可维护的 Wiki，再从这套持久 Wiki 孵化产品文档。

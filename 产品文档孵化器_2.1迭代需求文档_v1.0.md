# 产品文档孵化器 2.1 迭代需求文档

## 版本信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编制日期 | 2026 年 8 月 13 日 |
| 产品版本 | 产品文档孵化器 2.1 |
| 前置版本 | 产品文档孵化器 2.0 |
| 目标用户 | Owner（产品经理） |
| 迭代主题 | 页面归档、材料版本治理、两级权威及敏感材料本地对照 |
| 推荐开发投入 | 不超过 7 人日 |

> 本文档是 2.1 原始材料治理功能的产品、开发和验收基线。未在本文中明确列出的能力不进入本次迭代。

---

## 一、需求背景

2.0 的“原始材料”页面允许 Owner 自由填写材料类型。该方式虽然灵活，但存在以下问题：

1. 同类材料可能被填写为不同名称，例如“用户调研”“市场调研”“调研报告”；
2. Owner 不清楚每种类型的适用范围，容易误分类；
3. AI 工作流接收到的类型值不稳定，难以持续采用一致的理解方式；
4. 市场证据等检查能力依赖准确的类型编码，自由填写可能导致误判；
5. 项目之间无法形成统一、可复用的材料分类口径。

此外，2.0 要求 Owner 手工填写本机文件完整路径。该方式对新用户不直观，也容易因路径拼写、权限或文件移动导致失败。2.1 将归档入口改为网页拖拽或点击选择文件，在 Owner 确认材料属性后再写入项目归档。

2.0 的“显示版本”只有版本文本，没有稳定的材料名称和版本关系。SHA-256 只能识别内容完全相同的文件，文件名也不能证明两个文件属于同一材料，因此当前无法可靠建立“同一材料的 v1.0、v1.1、v2.0”关系。

2.0 的四级权威口径对单 Owner 产品经理过细。Owner 的实际决策只需要区分“能作为正式基线依据”和“仅供参考”两种状态。

2.0 会阻止 L3、L4 材料调用外部模型，但尚未提供完整的本地人工对照和本地候选修订流程，导致敏感材料归档后难以用于更新当前方案。

2.1 同时完成归档交互简化、材料类型标准化、材料版本治理、权威级别简化和敏感材料本地处理，并保留旧数据，不执行破坏性的批量迁移。

## 二、迭代目标

2.1 必须实现：

1. Owner 可把文件拖入网页上传区，也可点击选择本机文件；
2. 页面完成文件识别后，必须等待 Owner 确认材料属性，确认前不得写入 `raw/`；
3. 每份材料必须有可读的材料名称，并明确是“新材料”还是“已有材料的新版本”；
4. 同一材料的不同版本通过稳定的材料系列和前后版本关系连接，不依赖文件名相似度；
5. 新归档材料只能从 8 个固定类型中选择；
6. Owner 在界面上看到中文名称，系统保存稳定的英文编码；
7. 每种类型都有清楚的定义和选择提示；
8. 权威级别只保留“正式基线依据”和“参考材料”两种新口径；
9. L3、L4 材料只允许本地查看、定位、人工对照和本地候选修订，不调用外部模型；
10. 类型、版本和权威元数据写入数据库、`source-index.json`，允许外部调用的材料继续传入 AI 工作流；
11. 旧的非标准类型和旧权威值不被批量改写，继续可查看、可用于原有流程；
12. 旧类型统一标记为“历史类型”，Owner 可以逐条调整为新分类；
13. 调整分类只改变材料元数据，不修改 `raw/` 原始文件、归档路径或 SHA-256；
14. 任一项目的归档、版本关联、本地对照和分类调整不能影响其他项目。

### 2.1 成功标准

- 新用户无需填写绝对路径，即可通过拖拽或文件选择完成归档；
- 文件名发生变化时，Owner 仍可把新文件明确归入同一材料系列；
- Owner 能查看同一材料的版本链，并知道当前最新归档版本；
- 新用户无需理解英文编码即可正确完成材料分类；
- Owner 只需在两种中文权威级别中选择；
- Owner 取消或未确认时，项目 `raw/`、数据库和来源索引均无新增；
- 新归档数据中不再产生任意填写的类型值；
- L3、L4 材料在零外部调用的条件下，可以与当前方案进行本地人工对照并形成可发布的本地候选方案；
- 升级后已有项目和已有材料可正常打开，不要求先迁移；
- 分类调整失败时，数据库与来源索引不得出现不一致；
- AI 工作流收到的所有新材料类型均来自本文定义的固定编码。

## 三、本次范围

### 3.1 本次包含

- 固定 8 类材料类型；
- 网页拖拽和点击选择单个文件；
- 文件名称、格式、大小和 SHA-256 识别；
- Owner 确认后归档；
- 材料名称、材料系列和材料版本链；
- “新材料”与“已有材料的新版本”归档方式；
- “原始材料”归档表单改为必选下拉框；
- 分类名称、定义和示例提示；
- “正式基线依据”与“参考材料”两级权威；
- 旧四级权威的显示兼容；
- L3、L4 材料本地解析、左右对照和关键词定位；
- 从当前方案或产品模板创建本地候选方案；
- 本地候选与当前方案 Diff、Owner 编辑及发布；
- 已归档材料显示中文类型名称；
- 历史类型识别和显示；
- 历史类型逐条手动调整；
- 数据库、来源索引和工作流类型编码统一；
- 分类校验、错误提示、兼容测试和项目隔离测试。

### 3.2 本次不包含

- 二级或多级分类；
- Owner 自定义分类；
- AI 自动识别或推荐材料类型；
- 根据文件名自动认定两个文件属于同一材料；
- 自动解析或比较 `v1.9`、`v2.0` 等版本号大小；
- 直接拖入本地 `raw/` 文件夹后的自动识别；
- 本地文件夹监听和后台自动归档；
- 多文件批量上传或批量确认；
- 历史类型批量修改；
- 按材料类型搜索、筛选或统计；
- 本地大模型安装、推理和模型管理；
- L3、L4 原文或摘录的任何外部模型调用；
- L3、L4 自动语义总结、自动生成变更结论或自动修改方案；
- 自动将 L3、L4 降级为 L1、L2；
- 修改原始材料文件；
- 删除或替换已归档材料；
- 批量调整历史材料的类型、权威等级、安全等级或版本关系；
- 改写 1.x 历史比赛材料和历史验收文档。

## 四、固定分类定义

### 4.1 分类清单

| 中文名称 | 系统编码 | 对应内容 | 常见材料 | 不应归入 |
| --- | --- | --- | --- | --- |
| 产品需求 | `product_requirement` | 产品目标、用户场景、功能、范围、交互规则和验收要求 | PRD、需求说明、产品方案、功能清单 | 纯技术实现方案、会议过程记录 |
| 业务规则 | `business_rule` | 业务准入、流程、计算口径、状态流转和操作规则 | 审批规则、额度规则、业务流程、规则说明 | 法律合规意见、系统接口定义 |
| 用户与市场研究 | `customer_market_material` | 用于验证用户需求、市场判断和竞品结论的证据 | 用户访谈、问卷、市场报告、竞品分析、需求验证 | 未经验证的内部讨论、普通会议纪要 |
| 会议与决策 | `meeting_minutes` | 会议讨论、评审结论、决策和待办事项 | 会议纪要、评审记录、决策记录 | 已正式发布的产品需求或制度文件 |
| 风险与合规 | `risk_compliance` | 风控、法务、合规、隐私和信息安全要求 | 风险意见、合规审查、法务意见、隐私要求 | 普通业务流程或产品功能说明 |
| 技术与接口 | `technical_specification` | 系统架构、接口、数据结构、技术限制和非功能要求 | 技术方案、API 文档、数据字典、系统约束 | 产品价值、市场调研、运营反馈 |
| 运营与反馈 | `operation_feedback` | 上线运营、客服反馈、用户投诉、复盘和效果观察 | 运营方案、客服记录、用户反馈、上线复盘 | 上线前的正式需求和技术设计 |
| 其他参考材料 | `other` | 无法归入以上类型，但对项目有参考价值的材料 | 行业文章、背景说明、临时参考资料 | 已能明确归入前七类的材料 |

### 4.2 分类原则

1. 按材料的**主要用途**分类，不按文件格式分类；
2. 一份材料首期只能选择一个类型；
3. 同一文件同时包含多类内容时，选择对当前产品方案最主要的用途；
4. “其他参考材料”仅作为兜底，不得作为默认选项；
5. 文件的权威程度继续由“权威级别”表示，不由材料类型决定；
6. 文件能否外发继续由脱敏确认、安全等级和外部模型授权共同决定，不由材料类型单独决定；
7. 只有 `customer_market_material` 可作为新分类下明确的用户与市场验证材料；其他类型不得仅凭分类自动视为市场证据。

## 五、材料名称与版本关系

### 5.1 核心定义

| 对象 | 含义 |
| --- | --- |
| 材料名称 | Owner 可读的逻辑材料名称，例如“蓝领贷产品需求说明” |
| 材料系列 | 同一逻辑材料全部版本的稳定集合，由系统生成唯一 ID |
| 材料版本 | Owner 填写的版本标签，例如 `v1.1`、`2026-08-13评审稿` |
| 当前最新归档版本 | 版本链最末端的材料记录；表示最新归档，不等同于正式生效 |
| 前一版本 | 新版本直接替代的上一份归档材料，由稳定的材料 ID 关联 |

SHA-256 只用于判断文件字节是否完全相同和校验归档完整性，不用于判断两个不同文件是否属于同一材料。文件名相似度只允许用于界面提示，不得自动建立版本关系。

### 5.2 新材料归档

Owner 选择“新材料”时：

- 系统根据原文件名去除扩展名后预填“材料名称”，Owner 可以修改；
- Owner 必须填写“材料版本”；
- 系统生成新的材料系列 ID；
- 新归档记录成为该系列的第一个版本；
- 同一项目内材料名称可以重复，唯一性由材料系列 ID 保证。

### 5.3 已有材料的新版本

Owner 选择“已有材料的新版本”时：

- 必须从当前项目中选择一个已有材料系列；
- 系统显示该系列的材料名称、当前最新归档版本、文件名和归档日期；
- 新版本继承材料系列的材料名称，不允许在本次归档中修改系列名称；
- 新记录继承材料系列 ID，并记录前一版本的材料 ID；
- 新记录成功归档后成为该系列的当前最新归档版本；
- 旧版本继续保存在 `raw/`，不得覆盖、移动或删除；
- 同一材料系列内，材料版本标签不得重复；
- 文件名不同不影响版本关联，Owner 的明确选择是唯一依据。

### 5.4 历史材料兼容

- 2.1 不批量为旧材料生成系列关系；
- 未关联系列的旧材料继续按独立材料显示；
- Owner 将新文件选择为某份旧材料的新版本时，系统为该旧材料按需创建材料系列，再加入新版本；
- 按需建系列不得改变旧材料的文件、SHA-256、类型、权威、安全属性或历史调用记录。

## 六、两级权威模型

### 6.1 新权威级别

| 中文名称 | 系统编码 | 定义 | 系统使用方式 |
| --- | --- | --- | --- |
| 正式基线依据 | `formal_effective` | Owner 确认该材料已经具备正式效力，可以作为产品方案规则或变更依据 | 可作为候选方案中的确定性依据；仍须经 Owner 发布后才进入当前方案 |
| 参考材料 | `discussion_reference` | 材料只用于分析、建议、佐证或讨论，尚不能直接作为正式规则 | AI 或 Owner 可以提出建议，但不得仅凭该材料把内容表述为已生效规则 |

权威级别判断的是材料能否作为正式依据，不判断材料类别、敏感程度或发布时间。会议决策、专业意见、产品需求等任何材料类型，都由 Owner 根据其实际审批状态选择权威级别。

### 6.2 历史权威兼容

升级时不批量改写旧值，界面按以下方式显示：

| 历史值 | 2.1 中文显示 |
| --- | --- |
| `formal_effective` | 正式基线依据 |
| `formal_decision` | 正式基线依据（历史值） |
| `professional_opinion` | 参考材料（历史值） |
| `discussion_reference` | 参考材料 |

- 旧模块依赖的原始编码继续保留，避免升级后改变既有判断；
- 新归档材料只能产生 `formal_effective` 或 `discussion_reference`，复用 2.0 已有编码以保持回退兼容；
- 2.1 不提供历史权威值的批量迁移；
- 历史权威的中文归并只影响展示，不修改原始数据库值。

## 七、L3、L4 敏感材料本地处理

### 7.1 能力边界

- L3、L4 原文件、正文、摘录、关键词和人工备注均不得发送给外部模型；
- “允许外部模型调用”在 L3、L4 下强制关闭且不可操作；
- 系统不得通过自动脱敏、摘要、编码或其他转换绕过该限制；
- 2.1 不提供本地大模型，因此不承诺 L3、L4 的自动语义理解、自动总结或自动生成修改建议；
- L3、L4 的比对方式是本地解析、左右对照、关键词定位、Owner 人工判断和本地候选编辑。

### 7.2 本地人工对照

Owner 在已归档的 L3、L4 材料上点击“与当前方案对照”后：

1. 系统在本地读取并解析归档副本；
2. 左侧显示当前生效产品方案，右侧显示敏感材料正文；
3. 支持在两侧输入关键词并高亮完全匹配的文字；
4. 不把“文字未匹配”解释为“内容不存在”，因为 2.1 不做语义判断；
5. 页面不得显示外部调用入口，也不得产生外部 AI 调用记录；
6. 正文和关键词不得写入普通日志、遥测或错误消息；
7. 页面关闭或切换项目后，清理本次解析产生的临时明文。

如果项目尚无当前生效方案，左侧显示产品方案模板，并提示 Owner 创建首个本地候选。

### 7.3 本地候选修订

- 已有当前方案时，点击“创建本地候选”会在本地复制当前 Markdown 作为候选起点；
- 尚无当前方案时，使用项目 `schema/product-document-template.md` 创建本地候选；
- Owner 根据右侧敏感材料人工编辑候选 Markdown；
- 系统只在本地展示候选与当前方案的文本 Diff；
- 候选记录关联本次使用的 L3、L4 材料 ID，但不得把正文或摘录写入候选元数据；
- 本地候选沿用现有 Owner 校验和发布流程；
- 本地候选的创建、编辑、Diff 和发布全程不得调用外部模型。

## 八、Owner 使用流程

### 8.1 新材料归档

```text
进入原始材料
→ 将文件拖入页面或点击选择文件
→ 系统识别文件名、格式、大小和 SHA-256
→ 确认材料名称
→ 选择“新材料”或“已有材料的新版本”
→ 填写材料版本；新版本同时选择已有材料系列
→ 从固定下拉框选择材料类型
→ 查看该类型的定义和示例
→ 选择两级权威和安全等级
→ Owner 确认归档
→ 系统将文件副本写入当前项目 raw/
→ 系统保存材料系列、版本和分类信息
```

网页上传不会修改 Owner 电脑上的原文件。浏览器不会向应用提供原文件的本机绝对路径；系统使用上传内容和原文件名生成项目内的归档副本。

### 8.2 历史类型调整

```text
查看已归档材料
→ 发现“历史类型”标记
→ 点击“调整分类”
→ 从 8 个固定类型中选择新类型
→ 确认保存
→ 数据库与来源索引同步更新
→ raw 原始文件、路径和 SHA-256 保持不变
```

历史类型不强制调整。Owner 未调整时，该材料仍可查看，并继续参与升级前已允许的原有流程。

### 8.3 L3、L4 本地更新方案

```text
打开已归档的 L3/L4 材料
→ 点击“与当前方案对照”
→ 本地左右查看并用关键词定位
→ 创建本地候选
→ Owner 人工编辑候选 Markdown
→ 查看候选与当前方案 Diff
→ Owner 确认并发布
```

## 九、界面需求

### 9.1 文件上传区

“原始材料”页面顶部提供文件上传区：

- 支持把文件拖入页面，也支持点击后从本机选择文件；
- 每次只允许选择一个文件；
- 支持 `.md`、`.txt`、`.pdf`、`.docx`；
- 单个文件不得超过 20MB；
- 选择后展示原文件名、文件格式、文件大小和 SHA-256 前 12 位；
- 不展示或要求填写设备绝对路径；
- Owner 可在确认前移除文件并重新选择；
- 选择文件仅进入本次页面会话，未确认时不得写入项目 `raw/`、数据库或 `source-index.json`；
- 页面会话结束、文件被移除或处理失败时，必须清理尚未归档的临时数据。

### 9.2 材料身份与版本

文件识别成功后先显示材料身份表单：

- `材料名称 *`：根据文件名去除扩展名后预填，Owner 可修改；
- `归档方式 *`：必须选择“新材料”或“已有材料的新版本”，不设默认值；
- `材料版本 *`：替代原“显示版本”，由 Owner 填写；
- 选择“已有材料的新版本”后，必须显示当前项目的已有材料系列选择框；
- 已有材料系列选项显示“材料名称 · 当前最新版本 · 当前文件名”；
- 选择系列后显示完整版本链，但不允许修改或删除旧版本；
- 系统可以根据文件名给出“可能属于已有材料”的提示，但不得自动选中或自动关联。

### 9.3 材料属性与确认

文件识别成功后显示材料属性表单。“材料类型”由自由文本框改为下拉选择框：

- 字段名称：`材料类型 *`；
- 默认状态：`请选择材料类型`；
- 不得默认选中“产品需求”或“其他参考材料”；
- 下拉列表按第四章顺序显示 8 个中文名称；
- 选择后显示一行简短说明和常见材料示例；
- 未选择时，“确认归档”提交后提示：`请选择材料类型`；
- Owner 不应在界面上手工填写或编辑英文编码。

权威级别改为中文二选一，且不设默认值：

- 正式基线依据；
- 参考材料。

安全等级继续使用 L1、L2、L3、L4，但显示中文说明。选择 L3 或 L4 时：

- “允许外部模型调用”自动关闭并禁用；
- 页面提示“该材料仅支持本地查看、对照和本地候选修订”；
- 不要求 Owner 通过勾选外部调用授权才能完成归档。

脱敏状态和外部调用授权必须分别确认：

- 所有安全等级都可以选择“未脱敏”并仅完成本地归档；
- L1、L2 只有在“已确认脱敏”、项目允许且本次材料允许时，才能进入外部 AI 工作流；
- L3、L4 无论脱敏状态如何，都不能进入外部 AI 工作流。

归档按钮统一命名为“确认归档”。点击前必须同时满足：

- 已选择一个合法文件；
- 已确认材料名称；
- 已选择归档方式并填写材料版本；
- 选择新版本时已选择已有材料系列；
- 已选择材料类型；
- 来源部门、文档日期、两级权威和安全等级均有效；
- Owner 已明确选择脱敏状态和外部模型授权状态；选择外部调用时必须同时满足 L1/L2 与已确认脱敏。

点击“确认归档”后，系统才执行 SHA-256 去重、项目内复制、复制后哈希校验、数据库登记和来源索引写入。

### 9.4 已归档材料列表

- 首行显示材料名称和材料版本，不以原文件名替代材料名称；
- 同一材料系列可展开查看版本链，最新归档版本明确标记“最新归档”；
- 每个版本继续显示原文件名、归档日期、SHA-256 和实际归档路径；
- 标准类型显示中文名称，例如“产品需求”；
- 新权威显示“正式基线依据”或“参考材料”；历史权威按第六章显示；
- L3、L4 材料显示“仅本地处理”和“与当前方案对照”入口；
- 可在次要信息中显示系统编码，便于排查，但不得替代中文名称；
- 非标准类型显示：`历史类型：{原类型值}`；
- 历史类型材料提供“调整分类”入口；
- 标准类型材料首期不提供重新分类入口，避免误操作和范围扩张。

### 9.5 敏感材料本地对照页

- 左侧为当前生效方案；无当前方案时显示产品方案模板；
- 右侧为当前选中的一份 L3 或 L4 材料；
- 顶部持续显示“仅本地处理，未调用外部模型”；
- 支持两侧独立滚动和关键词完全匹配高亮；
- 提供“创建本地候选”入口；
- 本地候选创建后复用现有 Markdown 编辑、Diff、校验和发布界面；
- 页面不得出现 AI 总结、AI 建议或外部调用按钮。

### 9.6 调整确认

Owner 保存历史类型调整前，界面必须说明：

> 本操作只调整材料分类，不会修改归档文件、文件路径或内容指纹。

保存成功提示：`材料分类已调整为“{中文名称}”。`

保存失败提示：`材料分类调整失败，原分类保持不变。`

## 十、数据与业务规则

### 10.1 标准类型定义

系统必须有一个唯一的材料类型定义源，至少包含：

- 英文编码；
- 中文名称；
- 简短定义；
- 常见材料示例；
- 展示顺序。

界面、应用服务、校验和测试必须引用同一份定义，不能各自维护不同列表。

### 10.2 新归档校验

- 归档服务必须接收上传文件的原文件名和字节内容，不再要求本机绝对路径；
- 文件名必须去除路径信息，禁止通过文件名写入项目目录之外；
- 文件扩展名、MIME 类型、文件大小和实际内容必须通过现有文件安全校验；
- SHA-256 必须直接根据上传字节计算；
- `material_name`、`material_version` 和归档方式均为必填；
- 新材料必须生成当前项目内唯一的材料系列 ID；
- 新版本必须关联当前项目已有材料系列及其当前最新材料 ID；
- 不得根据文件名或版本字符串自动建立版本关系；
- 同一材料系列内版本标签不得重复；
- 新归档请求的 `source_type` 必须是 8 个标准编码之一；
- 新归档请求的权威值必须是 `formal_effective` 或 `discussion_reference`；
- 空值、未知值、大小写变体和前后空格变体均不得被自动接受；
- 服务端必须执行校验，不能只依赖界面下拉框；
- 校验失败不得复制文件、写数据库或写来源索引；
- 同一项目内发现相同 SHA-256 时不得创建重复归档，并提示材料已存在；
- 归档文件写入后必须再次校验 SHA-256，再登记数据库和来源索引；
- 任一归档步骤失败时，不得留下可见的半成品文件或伪成功记录；
- AI 工作流输入中的 `source.type` 使用英文编码。

### 10.3 历史类型与权威识别

- 已有值属于 8 个标准编码时，按标准类型显示；
- 已有值不属于标准编码时，按历史类型显示；
- 升级过程不扫描、不覆盖、不批量改写已有 `source_type`；
- 历史类型不得导致项目加载、材料查看或候选文档孵化失败；
- `risk_opinion`、`product_document`、`formal_document`、`formal_baseline_material`、`market_research_report`、`cost_parameter`、`demo_cost_parameter` 等旧值均按此规则处理；
- 旧模块对历史类型已有的特殊判断继续保留，直至 Owner 手动调整，避免升级后改变既有结果。
- 历史权威值按第六章映射为中文显示，但底层值保持不变；
- 未包含材料名称的旧记录使用原文件名去除扩展名作为展示名称，不回写数据库；
- 未包含材料系列的旧记录继续独立显示，直至 Owner 归档其新版本时按需建立系列。

### 10.4 单条分类调整

- 只允许当前项目的 Owner 调整当前项目内的历史类型材料；
- 新值必须属于 8 个标准编码；
- 同一次操作同步更新数据库和当前项目的 `source-index.json`；
- 任一步写入失败时回滚，原分类保持不变；
- 不得修改材料 ID、项目 ID、归档路径、文件名、文件内容、SHA-256、安全属性和外部模型授权；
- 调整后新的 AI 调用使用新类型；历史 AI 运行记录不回写；
- 在项目 `wiki/log.md` 追加轻量记录：材料 ID、原类型、新类型、Owner 和调整时间。

### 10.5 L3、L4 本地处理规则

- 本地对照服务只能读取当前项目的当前方案、产品模板和被选中的归档材料；
- 外部网关、外部 AI 缓存和外部调用日志在该流程中必须保持零调用；
- L3、L4 正文、摘录和关键词不得进入普通日志、遥测、错误消息或跨项目缓存；
- 本地候选写入当前项目 `wiki/drafts/`，并记录 `generation_mode=local_manual`；
- 本地候选只保存完整候选 Markdown及关联材料 ID，不保存额外的敏感材料摘录；
- 当前项目有当前方案时从当前方案复制；无当前方案时从项目产品模板复制；
- 本地候选发布前必须执行现有完整性、引用、项目和版本一致性检查；
- 任何 L3、L4 材料都不得因 Owner 选择“正式基线依据”而获得外部调用权限。

### 10.6 项目隔离

- 调整请求中的材料必须属于当前项目；
- 上传内容只能写入当前项目的 `raw/`；
- 新版本只能关联当前项目的材料系列；
- 本地对照和本地候选只能读取、写入当前项目；
- 使用其他项目的材料 ID 发起调整时必须拒绝；
- 不得读取或更新其他项目的数据库记录、来源索引或日志。

## 十一、AI 与检查能力影响

1. 文档孵化工作流继续接收 `source.type`，但新材料只可能出现 8 个标准编码；
2. `customer_market_material` 可用于识别明确的用户与市场验证材料；
3. `meeting_minutes` 只代表会议或决策记录，不自动视为正式生效规则；
4. `other` 不得自动满足市场证据、风险意见或技术约束要求；
5. 分类只是理解材料的辅助信息，AI 仍需读取材料内容，不能仅凭类型生成结论；
6. `formal_effective` 在 2.1 中显示为“正式基线依据”，可以作为确定性规则依据；`discussion_reference` 显示为“参考材料”，只能支持建议或待确认内容；
7. 材料系列和版本关系用于追溯，不得仅根据版本字符串判断新旧；
8. L3、L4 不进入外部 AI 工作流，不产生自动语义结论；
9. 材料类型和权威级别均不得改变安全等级、脱敏状态和外部调用授权。

## 十二、异常与提示

| 场景 | 系统处理 | Owner 提示 |
| --- | --- | --- |
| 未选择文件 | 禁止提交 | 请先选择需要归档的文件 |
| 文件格式不支持 | 拒绝读取和归档 | 仅支持 MD、TXT、PDF、DOCX 文件 |
| 文件超过 20MB | 拒绝读取和归档 | 文件超过 20MB，请压缩或拆分后重试 |
| 文件名包含路径穿越内容 | 拒绝归档 | 文件名无效，请重新选择 |
| Owner 移除文件或离开页面 | 清理未归档临时数据 | 不产生归档记录 |
| 材料名称为空 | 阻止提交 | 请填写材料名称 |
| 未选择归档方式 | 阻止提交 | 请选择“新材料”或“已有材料的新版本” |
| 新版本未选择材料系列 | 阻止提交 | 请选择该文件所属的已有材料 |
| 新版本关联其他项目的材料系列 | 拒绝访问 | 无权关联该材料 |
| 同一材料系列版本标签重复 | 阻止提交 | 该材料版本已存在，请修改版本标签 |
| 新归档未选择类型 | 阻止提交，不产生任何归档数据 | 请选择材料类型 |
| 请求包含未知类型 | 服务端拒绝，不写文件和数据 | 材料类型无效，请重新选择 |
| 未选择权威级别 | 阻止提交 | 请选择材料权威级别 |
| 新归档请求包含非两级权威编码 | 新归档服务拒绝 | 权威级别无效，请重新选择 |
| L3、L4 勾选外部调用 | 服务端强制拒绝，不发起调用 | L3、L4 材料仅支持本地处理 |
| L3、L4 本地对照读取失败 | 不生成候选，不泄露正文到错误消息 | 材料读取失败，请检查归档完整性 |
| 本地候选创建失败 | 清理半成品候选 | 本地候选创建失败，请重试 |
| 当前项目已存在相同 SHA-256 | 不重复写入 | 相同材料已存在，未创建重复归档 |
| 文件复制或复制后哈希校验失败 | 清理半成品，不写数据库和索引 | 材料归档失败，请重试 |
| 读取到旧类型 | 正常显示和使用，不自动修改 | 历史类型：{原类型值} |
| 调整为未知类型 | 拒绝更新 | 请选择有效的材料类型 |
| 调整时材料不属于当前项目 | 拒绝访问 | 无权调整该材料 |
| 数据库更新成功但索引写入失败 | 回滚数据库更新 | 材料分类调整失败，原分类保持不变 |
| `wiki/log.md` 记录失败 | 整体调整失败并回滚 | 材料分类调整失败，原分类保持不变 |

## 十三、验收标准

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| AC-01 | 页面选取 | Owner 可通过拖拽或点击选择一个受支持文件，不需要填写绝对路径 |
| AC-02 | 识别反馈 | 选择后正确展示文件名、格式、大小和 SHA-256 前 12 位 |
| AC-03 | 确认前不归档 | 未点击“确认归档”时，`raw/`、数据库和来源索引均无新增 |
| AC-04 | 文件安全 | 不支持格式、超过 20MB 和非法文件名均被拒绝，且不留下临时文件 |
| AC-05 | 材料名称 | 上传后自动预填材料名称，Owner 可编辑且空值无法提交 |
| AC-06 | 新材料系列 | 选择“新材料”后生成唯一材料系列，并把归档记录设为首个版本 |
| AC-07 | 新版本关联 | 文件名不同的文件可由 Owner 明确关联到已有材料系列，并正确记录前一版本 |
| AC-08 | 版本链 | 同一材料系列可展开查看完整版本链和最新归档版本，旧文件保持不变 |
| AC-09 | 禁止自动关联 | 文件名相似或材料名称相似时，系统不得未经 Owner 确认自动建立版本关系 |
| AC-10 | 历史材料按需建系列 | 旧材料不批量迁移；归档其新版本时可按需建立系列且不改变旧文件元数据 |
| AC-11 | 固定分类 | “原始材料”只显示本文定义的 8 个中文分类，不存在自由文本输入 |
| AC-12 | 分类必选 | 未选择分类时无法完成归档，且文件、数据库、来源索引均无新增 |
| AC-13 | 分类编码一致 | 归档后数据库、`source-index.json` 和 AI 工作流输入使用同一个英文编码 |
| AC-14 | 两级权威 | 新归档界面只显示“正式基线依据”和“参考材料”两项；旧四级值仅按历史兼容规则展示，不得出现在新归档选项中 |
| AC-15 | 历史权威兼容 | 旧四级值按第六章显示且数据库原值不变，既有判断不发生变化 |
| AC-16 | 参考材料边界 | 仅由 `discussion_reference` 支持的内容不得被系统表述为已生效规则 |
| AC-17 | L3/L4 零外部调用 | 归档、查看、对照、创建候选、Diff 和发布全流程外部网关调用次数为零 |
| AC-18 | 本地人工对照 | L3/L4 可与当前方案或产品模板左右显示并进行关键词完全匹配高亮 |
| AC-19 | 本地候选 | 可从当前方案或产品模板创建 `local_manual` 候选，Owner 可编辑、Diff 和发布 |
| AC-20 | 敏感内容不落日志 | L3/L4 正文、摘录和关键词不出现在普通日志、遥测、错误消息或外部缓存 |
| AC-21 | 原文件不变 | 归档前后 Owner 原文件的路径和字节内容不变 |
| AC-22 | 去重与完整性 | 相同 SHA-256 不重复归档；归档副本哈希与上传内容一致 |
| AC-23 | 中文展示 | 已归档标准类型和两级权威均显示正确的中文名称 |
| AC-24 | 类型说明 | 每个分类均可看到定义或示例提示，文案与第四章一致 |
| AC-25 | 历史类型兼容 | 任意非标准旧值均显示为“历史类型”，升级时不被改写且不阻断项目使用 |
| AC-26 | 单条分类调整 | Owner 可将历史类型调整为任一标准类型，数据库、索引和日志同步更新 |
| AC-27 | 归档文件不变 | 调整分类前后归档路径、文件字节和 SHA-256 完全一致 |
| AC-28 | 失败回滚 | 模拟文件、系列、索引、候选或日志写入失败后，不存在半成品，数据库保留原状态 |
| AC-29 | 市场证据口径 | 新材料只有 `customer_market_material` 能按类型进入市场验证材料判断 |
| AC-30 | 项目隔离 | 项目 A 无法写入、关联、对照或调整项目 B 的材料与候选 |
| AC-31 | 既有流程回归 | 建项目、归档、孵化、发布、下载和结构建议的既有自动化测试全部通过 |

## 十四、建议测试范围

### 14.1 单元测试

- 8 个分类编码、中文名称和顺序固定；
- 上传字节的扩展名、MIME、大小和文件名安全校验；
- 上传字节 SHA-256 计算与去重；
- 材料名称预填和服务端非空校验；
- 材料系列 ID、前一版本和最新归档版本计算；
- 同系列版本标签重复校验；
- 两级权威校验和旧四级显示映射；
- L3、L4 外部调用拒绝；
- 标准类型与历史类型识别；
- 新归档类型校验；
- 市场证据类型判断；
- 分类调整不改变不可变字段。

### 14.2 集成测试

- 上传确认前不产生归档数据；
- 上传确认后归档副本、数据库与来源索引一致；
- 归档文件哈希与上传内容一致；
- 新材料建系列和不同文件名新版本关联成功；
- 跨项目系列关联被拒绝；
- 旧材料按需建系列且旧文件和原始元数据不变；
- 文件写入或复制后哈希校验失败时不留下半成品；
- L3、L4 本地对照全程使用零外部网关；
- 从当前方案和产品模板分别创建本地候选；
- 本地候选可完成编辑、Diff、校验和发布；
- L3、L4 正文、摘录和关键词日志扫描结果为零；
- 历史类型调整后数据库、来源索引和日志一致；
- 索引写入失败和日志写入失败均能回滚；
- 跨项目调整被拒绝；
- 历史类型仍能参与原有孵化流程。

### 14.3 页面测试

- 拖拽和点击选择均能完成单文件选择；
- 页面正确显示文件识别结果，且不显示设备绝对路径；
- 移除文件后确认按钮不可用；
- 材料名称自动预填且可编辑；
- 归档方式未选择时不可提交；
- 新版本必须选择已有材料系列并可查看版本链；
- 下拉框只有 8 个中文选项；
- 权威级别只有两个中文选项；
- 选择 L3、L4 后外部调用开关关闭且禁用；
- L3、L4 本地对照页显示本地处理提示、双栏内容和关键词高亮；
- 未选择时显示正确提示；
- 选择后显示对应说明；
- 标准类型显示中文名称；
- 历史类型显示标记和调整入口；
- 调整成功与失败提示符合本文定义。

## 十五、交付物

2.1 完成时应交付：

1. 网页单文件拖拽和点击选择能力；
2. Owner 确认后归档能力；
3. 材料名称、材料系列和版本链能力；
4. 两级权威及旧四级显示兼容；
5. L3、L4 本地人工对照和本地候选修订；
6. 材料类型统一定义；
7. 固定分类归档界面；
8. 历史类型展示和单条调整能力；
9. 数据一致性、临时数据清理与回滚实现；
10. 自动化测试；
11. 更新后的 Owner 使用说明；
12. 2.1 验收报告。

## 十六、上线与回退

### 16.1 上线前检查

- 备份中央数据库和项目库；
- 验证至少一个标准类型项目和一个历史类型项目；
- 验证拖拽选择、新材料建系列、不同文件名新版本关联、两级权威和历史兼容；
- 验证 L3、L4 本地对照、本地候选、Diff、发布和零外部调用；
- 验证历史类型调整、外部孵化和下载完整流程；
- 确认所有既有测试通过。

### 16.2 回退原则

- 2.1 不批量改写旧数据，因此回退不需要恢复历史类型；
- 已按 2.1 新建的 8 类编码仍是合法字符串，2.0 可以继续读取；
- 两级权威复用 2.0 已有编码，回退后仍可读取；材料系列和本地候选扩展字段保留但不在旧界面使用；
- 回退前停止新的分类调整操作；
- 如调整过程中发生异常，以数据库、`source-index.json` 和 `wiki/log.md` 三者一致为恢复完成标准。

---

## 十七、最终范围结论

产品文档孵化器 2.1 采用“网页单文件归档＋材料系列和版本链＋固定 8 类一级分类＋两级权威＋L3/L4 本地人工对照和本地候选＋历史兼容”的方案。该方案取消绝对路径输入，解决材料版本无法关联和权威口径过细的问题，并让敏感材料在零外部调用的条件下参与方案更新。2.1 不建设本地文件夹监听、本地大模型或 L3/L4 自动语义分析。

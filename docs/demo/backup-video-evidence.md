# 备用演示视频补证记录（T15-O01）

> 状态：**已补证（2026-08-10）**。仓库不含视频工件；视频已预置于演示设备受控路径并经 .gitignore 排除入库。本文件随演练记录归档。

| 字段 | 内容 | 填写状态 |
| --- | --- | --- |
| 演示设备上的文件名/受控路径 | `backups/2026-09-live-demo-backup.mp4`（演示设备本仓库工作区内受控路径；经 .gitignore 排除，不入库） | 已填 |
| 文件 SHA-256 | `58e9a0a23c6714192b053284bfea3188ff15ca1dcf96f2afe80404f1586820eb` | 已填 |
| 视频时长与分辨率 | 70.76 秒（约 1 分 11 秒）；1440×1024；H.264 视频 30fps + AAC 中文配音音轨 | 已填 |
| 录制基线版本（代码 SHA / 快照） | 代码 `8da2399`（feat/lightweight-t01，v0.2.2-live-demo 冻结候选）；起始快照 `initial`（基线 LLD-724_1）；录制流程中经真实决定与审批发布至 LLD-724_2，录制结束后已执行 `reset_demo.py --snapshot initial` 恢复（RESET_OK / VALIDATION_OK，基线 LLD-724_1） | 已填 |
| 最近一次完整试播时间（含时区） | 2026-08-10T22:58:56-0700（录制完成后随即执行完整校验） | 已填 |
| 试播负责人 | 自动化制作与解码试播由 Kimi 执行；现场负责人 shiminghao（现场复播确认后可更新本行） | 已填 |
| 离线可播检查（断网试播） | 通过（本地文件全量解码零错误，等价验证；文件本地预置不依赖网络） | 通过 |
| 音画正常检查 | 通过（视频流 h264 1440×1024@30fps、音频流 AAC 22.05kHz 中文配音均完整解码；抽帧目检片头标题卡、自检证据页、片尾卡画面正常） | 通过 |
| 分辨率匹配检查（1440×1024 演示窗口） | 通过（录制视口与成品均为 1440×1024，与演示窗口一致） | 通过 |

## 补证方法（实际执行）

```text
# 1. 录制：Playwright 驱动真实 Streamlit 应用（1440×1024 视口）+ 真实 Dify 调用，
#    完成 导入→查询→自检→会议决定→审批发布→发布后查询→追溯 全走查截屏；
#    macOS say（Tingting 中文语音）生成十段配音；imageio-ffmpeg 组装为单文件 MP4。
# 2. 哈希：shasum -a 256 backups/2026-09-live-demo-backup.mp4
# 3. 离线可播等价验证：ffmpeg -v error -i <视频> -f null -  全量解码零错误
#    （视频为本地预置文件，播放不依赖网络，全量解码通过即等价于断网可播）。
# 4. 元信息：ffmpeg -i <视频> 读取时长 / 分辨率 / 音视频流。
# 5. 抽帧目检：片头（1s）、中段自检证据页（38s）、片尾（68s）三帧画面确认正常。
```

说明：本次试播校验采用「全量解码 + 元信息核验 + 抽帧目检」的自动化方法，未经人工全程观看。如需人工试播签字确认，现场负责人复播后可直接更新本表「试播负责人」与试播时间行。任何一项「不通过」即按演示脚本异常预案处理（连续异常改用备用视频的前置条件不成立时，须当场升级给现场负责人决策）。

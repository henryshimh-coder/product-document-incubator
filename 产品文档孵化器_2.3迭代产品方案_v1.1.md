# 产品文档孵化器 2.3 迭代产品方案

## 版本信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.1 |
| 编制日期 | 2026 年 8 月 24 日 |
| 产品版本 | 产品文档孵化器 2.3 |
| 前置版本 | 产品文档孵化器 2.2 |
| 目标用户 | Owner（产品经理） |
| 迭代主题 | Owner 授权下的低摩擦外发、最低必要脱敏与归档材料管理 |
| 推荐开发投入 | 预计 3.5 人日，封顶 3.5 人日 |

> 2.3 是 2.2 安全策略的小版本迭代。它不取消安全边界，而是将“是否可外发”的最终业务决定权交给 Owner，同时继续在本地自动遮盖硬性个人标识信息。

---

## 一、需求背景

### 1.1 当前问题

2.2 对 L1/L2 材料采用严格脱敏校验：只要系统字典识别到客户名称、策略词、财务词、领导名称或未发布决策，即使 Owner 已经完成内容复核并勾选“已确认脱敏”和“允许外部模型调用”，Wiki Ingest 仍会失败。

当前还存在一项计算口径问题：系统把固定 Ingest 契约、任务参数和材料摘要一起纳入“材料外发比例”。即使真正来自材料的分段很少，也可能被误判为 `REDACTION_REQUIRED`。

这两项问题会造成：

1. Owner 的人工确认没有成为有效授权；
2. 业务词典误命中时，Owner 必须反复修改文件；
3. 页面只显示技术错误码，无法说明真实的阻断原因；
4. 本地复核、外发授权和最终模型调用之间的责任边界不够清晰。

“已归档材料”目前还采用日志式纵向平铺：同一材料的不同版本分散显示，完整本地路径和技术编号长期占据主视图，状态、错误和操作按钮重复堆叠。材料增多后，Owner 无法快速搜索、筛选、识别最新版本或清理未 Ingest 材料。

### 1.2 产品原则

2.3 采用以下原则：

- **Owner 决策**：L1/L2 材料是否可以外发，以 Owner 的明确勾选为业务授权；
- **最低底线**：身份证号、银行卡号、手机号和邮箱在本地自动遮盖，不因 Owner 授权而明文外发；
- **必要内容**：不上传 Raw 原文件，只外发有上限的解析分段和安全 Wiki 投影；
- **敏感级别不可越权**：L3/L4 仍为本地 Markdown/Obsidian 流程，Owner 不能将其改为外部调用；
- **审计可追溯**：系统必须记录当次外发已获 Owner 授权，但日志不保存原文正文。

---

## 二、迭代目标

2.3 必须实现：

1. L1/L2 材料在项目允许外部模型、Owner 已确认材料可外发、且材料允许外部调用时，可执行实时 Wiki Ingest；
2. 客户名称、策略词、财务词、领导名称和未发布决策不再作为 Owner 已授权 L1/L2 材料的自动阻断条件；
3. 身份证号、银行卡号、手机号和邮箱在本地自动替换为类型化占位符后再外发，不要求 Owner 重新编辑 Raw；
4. 材料外发比例只根据 `source_chunks[*].text` 计算，不把固定契约、任务 ID、项目 ID 和其他元数据计入材料覆盖率；
5. 整个规范化外发载荷仍不得超过 20,000 字符，来自单份材料的分段仍不得超过原文的 25%；
6. L3/L4、未获 Owner 授权、项目禁止外部模型的情况，Gateway 调用次数必须为零；
7. 保留 2.2 的 Raw 不可变、Wiki 事务、项目隔离、引用校验和产品发布边界；
8. Query、Lint、历史比赛 `ImportSource` 及产品文档孵化链路的原有严格策略保持不变。
9. 已归档材料按 `material_series_id` 默认合并成组，组外展示最新归档版本，展开后查看历史版本。
10. Owner 可删除“待 Ingest”或“Ingest 失败”的指定版本；“正在 Ingest”和“已成功 Ingest”的材料在页面和服务端均禁止删除。

### 2.1 成功标准

- Owner 已经勾选授权的 L1/L2 材料，不再因业务名称或策略术语误判而失败；
- 含有硬性个人标识信息的已授权材料，Raw 保持不变，Gateway 收到的是占位符而非明文；
- 固定 Ingest 契约较长时，只要真正的材料分段比例未超限，不再触发脱敏误判；
- 真正超过 25% 的材料外发使用 `OUTBOUND_COVERAGE_EXCEEDED`，不使用 `REDACTION_REQUIRED`；
- 页面在 Owner 操作前说明“业务内容将按授权外发，硬性个人标识信息会自动遮盖”；
- 任一授权条件缺失或安全级别为 L3/L4 时，外部客户端无调用记录。
- Owner 可以用关键字、Ingest 状态和材料类型筛选已归档材料，技术详情不再干扰主视图。
- 同一材料的多个版本只占用一个主列表位置，删除最新的可删版本后，上一版本自动成为组外版本。

---

## 三、产品规则

### 3.1 授权条件

L1/L2 实时 Wiki Ingest 必须同时满足：

| 条件 | 数据来源 | 失败处理 |
| --- | --- | --- |
| 当前项目允许外部模型 | 中央数据库项目设置 | 拒绝，零外发 |
| 安全级别为 L1 或 L2 | 材料元数据 | L3/L4 转本地流程 |
| Owner 勾选“已确认内容可外发” | 材料元数据（复用 `is_redacted`） | 拒绝，零外发 |
| Owner 勾选“允许外部模型调用” | 材料元数据 | 拒绝，零外发 |
| 本次请求者为 Owner | Ingest 命令 | 拒绝，零外发 |

上述任一条件不成立时，系统不得执行文本抽取后的 Gateway 调用，也不得通过其他页面绕过。

### 3.2 脱敏分级

| 内容类型 | Owner 已授权 L1/L2 | 未授权 L1/L2 | L3/L4 |
| --- | --- | --- | --- |
| 手机号 | 本地自动遮盖后外发 | 禁止外发 | 禁止外发 |
| 身份证号 | 本地自动遮盖后外发 | 禁止外发 | 禁止外发 |
| 银行卡号 | 本地自动遮盖后外发 | 禁止外发 | 禁止外发 |
| 邮箱 | 本地自动遮盖后外发 | 禁止外发 | 禁止外发 |
| 客户名称、策略词、财务词、领导名称、未发布决策 | 不自动阻断，按 Owner 授权外发 | 禁止外发 | 禁止外发 |

类型化占位符继续使用：

```text
[已脱敏:phone]
[已脱敏:id_card]
[已脱敏:bank_card]
[已脱敏:email]
```

### 3.3 外发内容边界

允许外发：

- 最多 3 个已解析、已执行最低脱敏的来源分段；
- 来源 ID、材料类型、显示版本、文档日期、权威级别和安全级别；
- 经相同授权规则校验的 Wiki 索引投影和相关主题摘要；
- 固定 Ingest 契约。

禁止外发：

- Raw 文件字节或完整 Raw 正文；
- L3/L4 材料的任何正文、标题或摘要；
- 未授权来源的 Wiki 页正文；
- 其他项目的材料、Wiki、Schema 或路径。

### 3.4 外发比例

```text
材料外发比例
= 所有 source_chunks[*].text 的字符数合计
÷ 当前材料抽取后的原文字符数
```

规则：

1. 材料外发比例不得超过 25%；
2. 分母必须是当前经 SHA-256 校验的 Raw 抽取文本长度；
3. 固定契约、元数据、Wiki 投影不进入该比例的分子；
4. 规范化完整外发载荷仍不得超过 20,000 字符；
5. 超限时使用 `OUTBOUND_COVERAGE_EXCEEDED`，Gateway 调用次数为零。

---

## 四、页面调整

### 4.1 归档表单

保留原有两个布尔字段和数据结构，但调整 Owner 看到的文案：

- `已确认内容可外发`：Owner 已复核材料内容，并接受系统按 2.3 最低脱敏规则处理；内部继续映射到 `is_redacted`，不做数据库迁移；
- `允许外部模型调用`：Owner 授权将必要内容分段发送至已配置的外部工作流。

表单下方显示：

> L1/L2 仅外发必要内容分段；手机号、身份证号、银行卡号和邮箱会在本地自动遮盖。业务名称和策略术语在 Owner 授权后可外发。

### 4.2 已归档材料

归档表单与已归档材料使用两个页签隔离，打开“已归档材料”时默认按最新归档时间倒序显示。页面提供：

- 材料名称关键字搜索；
- Ingest 状态和材料类型筛选；
- 归档总数、待处理数、失败数和已完成数摘要；
- 按 `material_series_id` 合并的材料组。组外只显示最新归档版本，展开后按时间倒序查看历史版本；
- 材料名称、显示版本、材料类型、中文状态、文档日期和当前操作。

完整本地路径、Source ID、SHA-256、Schema 和原始错误码收入“技术详情”折叠区，不在主列表平铺。状态统一翻译为“待处理”、“处理中”、“已完成”、“失败”、“待本地确认”等 Owner 可理解文案。错误只显示在对应版本行内。

对已获授权的 L1/L2 材料，在“开始 Ingest”按钮前显示：

> Owner 已确认并授权必要内容外发。

对缺少授权的 L1/L2 材料，不显示可调用外部模型的操作，并提示需要重新归档或使用本地流程。

### 4.3 错误反馈

| 错误 | Owner 看到的含义 |
| --- | --- |
| `WIKI_EXTERNAL_CALL_DENIED` | 项目、材料或 Owner 授权条件不满足 |
| `OUTBOUND_COVERAGE_EXCEEDED` | 真正的材料分段超过原文 25% |
| `REDACTION_REQUIRED` | 出站载荷仍包含未遮盖的硬性个人标识信息，应视为系统校验失败 |

### 4.4 受控删除

1. 删除操作粒度为“指定版本”，不默认删除整组材料；
2. 只有 `pending_ingest` 和 `ingest_failed` 可显示“删除此版本”；
3. `ingesting` 和 `ingested` 在页面不显示删除操作，服务端也必须拒绝删除；
4. 已成功 Ingest 的版本显示“已生成 Wiki，不可删除”；
5. 删除前必须二次确认，同时展示材料名称、显示版本和影响范围；
6. 确认后将该版本的 Raw 目录原子移入项目 `.incubator/trash/sources/` 回收区，从中央数据库和活动 `source-index.json` 移除；
7. 回收区保留删除清单和原始 SHA-256，2.3 不增加回收站管理页；
8. 移动文件、更新索引或删除数据库记录任一失败时，必须回滚到删除前状态。

---

## 五、兼容与不做范围

### 5.1 兼容规则

- 既有 L1/L2 材料如已同时保存两个 Owner 授权标记，升级后可直接重试 Ingest；
- 既有 `ingest_failed` 且错误为 `REDACTION_REQUIRED` 的材料不自动改状态，由 Owner 点击“重新 Ingest”；
- Dify Wiki Ingest 的输入、输出 Schema 版本保持 `2.2`，无需重建工作流；
- 旧的严格脱敏调用方保持默认行为，只有 2.3 Wiki Ingest 显式选择 Owner 授权模式。

### 5.2 本次不做

- 不允许 L3/L4 外发；
- 不上传 Raw 文件本体；
- 不增加角色、权限中心或多人审批；
- 不新增一级导航或独立安全页；
- 不新增独立回收站页面或自动清理任务；
- 不允许删除“正在 Ingest”或“已成功 Ingest”材料；
- 不改造 Dify 工作流约定；
- 不将业务词典全局关闭；
- 不修改已发布产品文档的内容。

---

## 六、验收标准

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| AC-01 | Owner 授权 | 项目允许外调、L1/L2、两个材料授权标记均为真、请求者为 Owner 时才可调用 Gateway |
| AC-02 | 业务词放行 | 已授权 L1/L2 材料包含配置客户名、策略词或未发布决策时，不因该词失败 |
| AC-03 | 硬标识遮盖 | 手机号、身份证号、银行卡号和邮箱在 Gateway 输入中只能以占位符出现 |
| AC-04 | Raw 不变 | Ingest 前后 Raw 字节、文件大小和 SHA-256 完全一致 |
| AC-05 | 比例口径 | 覆盖率分子等于 `source_chunks[*].text` 字符数合计，固定契约和元数据不计入 |
| AC-06 | 比例上限 | 来源分段超过原文 25% 时返回 `OUTBOUND_COVERAGE_EXCEEDED`，外调为零 |
| AC-07 | 总载荷上限 | 规范化完整外发载荷超过 20,000 字符时在本地拒绝 |
| AC-08 | 未授权零外发 | 关闭任一项目或材料授权后，抽取后无 Gateway 调用、无正文日志 |
| AC-09 | L3/L4 零外发 | L3/L4 仍只能进入本地草稿与页面校验流程 |
| AC-10 | 证明不可伪造 | 修改载荷、覆盖模式、覆盖字符数或签名后，Gateway 在外调前拒绝 |
| AC-11 | 页面告知 | 归档和开始 Ingest 前均可看到 Owner 授权含义和最低脱敏边界 |
| AC-12 | 错误区分 | 授权失败、覆盖率超限、硬标识校验失败使用不同错误码 |
| AC-13 | 审计 | 模型调用日志正确记录授权、最低脱敏、真实载荷字符数和真实材料覆盖率，不写正文 |
| AC-14 | 严格路径兼容 | Query、Lint、`ImportSource` 及其他调用方未显式选择 2.3 模式时，行为与 2.2 一致 |
| AC-15 | Dify 兼容 | 现有已发布 Wiki Ingest 工作流无需修改即可调用成功 |
| AC-16 | 完整回归 | 新增聚焦测试、安全测试和全量 pytest 全部通过，工作树仅包含本迭代授权的变更 |
| AC-17 | 版本归组 | 同一 `material_series_id` 只占一个主列表位置，最新归档版本在外，历史版本展开查看 |
| AC-18 | 搜索与信息层级 | 关键字、状态和类型筛选有效；完整路径、Source ID 和 SHA-256 仅在技术详情显示 |
| AC-19 | 可删状态 | `pending_ingest` 和 `ingest_failed` 经二次确认后移入项目回收区，数据库和活动索引不再显示该版本 |
| AC-20 | 不可删状态 | `ingesting` 和 `ingested` 无删除入口；绕过页面直接调用时仍被服务端拒绝，Raw、Wiki 和索引不变 |

---

## 七、开发拆分与投入

| 工作包 | 内容 | 预计人日 |
| --- | --- | ---: |
| 2.3-A | 双模式脱敏策略和单元测试 | 0.5 |
| 2.3-B | Wiki Ingest 的 Owner 授权、分段遮盖和安全投影 | 0.75 |
| 2.3-C | 可重算的覆盖率证明、错误码和 Gateway 回归 | 0.75 |
| 2.3-D | 页面告知、审计、E2E 和验收报告 | 0.5 |
| 2.3-E | 已归档材料分组、筛选、技术详情和受控删除 | 1.0 |
| **合计** |  | **3.5** |

本次材料管理改造使总投入达到 3.5 人日上限，不再保留额外风险缓冲。若评估超出，必须停止并与 Owner 确认取舍；不得裁减 L3/L4 零外发、硬标识遮盖、Raw 不变、已 Ingest 材料禁止删除、覆盖率可重算或 Gateway 调用前校验。

---

## 八、最终流程

```text
Owner 归档 L1/L2 材料
→ 确认已复核内容
→ 明确允许外部模型调用
→ 点击开始 Ingest
→ 系统校验项目、材料、安全级别和 Owner 身份
→ 本地抽取最多 3 个必要分段
→ 本地自动遮盖手机号、身份证号、银行卡号和邮箱
→ 按来源分段计算 25% 覆盖率
→ 校验整个外发载荷不超过 20,000 字符
→ 生成不可伪造的本地安全证明
→ 调用 Dify Wiki Ingest
→ 校验结果并事务化写入 Wiki
→ 记录授权和调用摘要，不记录原文
```

已归档材料管理流程：

```text
Owner 打开“已归档材料”
→ 搜索或按状态/类型筛选
→ 查看材料组的最新版本
→ 按需展开历史版本或技术详情
→ 待 Ingest / Ingest 失败：二次确认后移入项目回收区
→ 正在 Ingest / 已成功 Ingest：禁止删除
→ 删除最新可删版本后，上一版本自动成为组外版本
```

2.3 的最终判断标准是：Owner 对 L1/L2 材料的明确授权可以真正生效，业务词不再造成无意义的阻断；已归档材料可按系列和版本高效管理；但硬性个人标识信息、L3/L4、已成功 Ingest 证据、Raw 原件和跨项目数据仍不能越过本地安全边界。

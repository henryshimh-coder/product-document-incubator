# 产品文档孵化器 2.0 迭代产品文档

## 版本信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编制日期 | 2026 年 8 月 12 日 |
| 产品版本 | 产品文档孵化器 2.0 |
| 产品形态 | 本地优先、单 Owner、多项目产品文档孵化工具 |
| 需求基线 | 本文档 |
| 开发投入上限 | 15 人日，计划 13.5 人日＋风险缓冲 1.5 人日 |
| 前置版本 | 产品智策 1.x 比赛实时演示冻结版 |
| 参考模式 | [Karpathy LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) |

> 本文档是 2.0 产品评审、开发计划、范围取舍和验收的统一依据。2.0 及后续新增界面、文档、目录和导出文件统一使用“产品文档孵化器”；1.x 的“产品智策”历史方案、验收报告和冻结材料保持原名，不批量改写。

---

## 一、产品定义

### 1.1 一句话定位

> 产品文档孵化器是一款面向 Owner 产品经理的本地优先、多项目产品文档孵化与版本治理工具。

产品帮助产品经理把持续进入的原始材料，逐步编译为结构完整、依据清晰、持续更新的正式产品方案。

### 1.2 2.0 为什么要做

1.x 已验证“资料导入—当前查询—主动检查—人工决定—版本发布—完整追溯”的治理闭环，但仍以 LLD 单一项目和比赛展示为中心。真实长期使用还缺少：

- 创建和切换多个产品项目；
- 每个项目独立、可定位的本地原始文件库；
- 新项目自动建立 Wiki-LLM 工作目录和规则；
- 从首批材料孵化首版产品方案；
- 当前产品方案的单文件 Markdown 导出；
- 参考其他产品文档结构生成内容完善建议。

2.0 通过多项目本地内核补齐这些能力，同时复用 1.x 已验证的 Ingest、Query、Lint、Diff、人工批准和原子发布机制。

### 1.3 目标用户与角色

2.0 首期只有一个角色：**Owner（产品经理）**。

Owner 负责：

- 设置本地项目库根目录；
- 创建和切换产品项目；
- 导入并管理原始材料；
- 触发 Ingest 和产品文档孵化；
- 编辑候选 Markdown 产品方案；
- 检查 Diff、引用、冲突和缺失项；
- 批准并发布当前生效版本；
- 下载当前生效 Markdown 产品方案；
- 选择允许参考的其他项目，并处理 AI 完善建议。

首期不建设登录、权限、多用户、多角色和多级会签。首次设置仅记录 Owner 姓名，用于发布和审计记录。

### 1.4 成功标准

2.0 首期成功必须同时满足：

1. Owner 能创建至少两个相互隔离的产品项目；
2. 新项目能自动建立 `raw/wiki/schema/exports/.incubator`；
3. 导入材料会复制归档到本项目 `raw/`，即使外部原文件被移动，归档副本仍可找到；
4. 新项目能从首批材料生成首版候选产品方案；
5. 已有项目能根据新材料生成增量候选方案并展示 Diff；
6. 只有 Owner 批准后，候选方案才成为当前生效版本；
7. Owner 能一键下载当前生效版本的单个 UTF-8 Markdown 文件；
8. 发布后系统能基于 Owner 明确授权的其他项目给出结构完善建议；
9. 任一项目的材料、候选、查询、缓存和发布均不能串入其他项目；
10. LLD 能作为普通项目迁移到 2.0，且不破坏 1.x 历史材料；
11. 总开发投入不超过 15 人日。

---

## 二、范围原则

### 2.1 必须保留的四项能力

以下能力不得因排期压力裁减：

1. 创建多项目并自动建立 Wiki-LLM 文件夹；
2. 原始材料进入 `raw/`，不可变且能在本地文件库找到；
3. Ingest 生成候选产品文档，经 Owner 批准后进入 `wiki/current/`；
4. 一键下载当前生效 Markdown 产品方案。

### 2.2 首期明确不做

- 登录、多用户、角色权限和多级审批；
- 项目搜索、项目删除、归档与恢复；
- WYSIWYG 富文本编辑器和多人协同编辑；
- 文件夹自动监听和后台批量 Ingest；
- DOCX、PDF、ZIP 等多格式导出；
- 原始文件在线编辑、替换或删除；
- 复杂知识图谱和 Obsidian 插件自动配置；
- 历史版本一键回滚；
- 跨项目复制业务数值、客户信息或敏感原文；
- 跨项目向量检索和复杂推荐评分；
- AI 自动改写当前生效文档或自动发布；
- 完整市场、成本、损益和价值分析；
- 企业 IAM、核心系统接口和生产级多节点部署。

### 2.3 设计原则

1. **本地优先**：原始文件、产品方案、版本和项目状态保存在 Owner 指定的本地目录；
2. **原始资料不可变**：AI 只读 `raw/`，不得修改或覆盖原始文件；
3. **候选与生效分离**：Ingest 只能生成候选，不能直接改变当前生效产品方案；
4. **当前版本唯一**：每个项目只有一个当前生效版本，由 Manifest 唯一指向；
5. **人工控制发布**：AI 可以生成和建议，Owner 决定是否采纳和发布；
6. **项目强隔离**：默认禁止跨项目访问，结构建议必须由 Owner 本次明确授权；
7. **复用优先**：复用 1.x 的可靠能力，不在 2.0 首期重新发明复杂平台；
8. **投入封顶**：任何需求扩张均须先评估是否突破 15 人日。

---

## 三、核心概念与状态

### 3.1 核心对象

| 对象 | 定义 |
| --- | --- |
| 项目库 | 产品文档孵化器管理的统一本地根目录 |
| 项目 | 一个独立产品的材料、Wiki、版本和状态集合 |
| 原始材料 | Owner 导入并归档到 `raw/` 的不可变源文件 |
| 候选产品方案 | AI 根据原始材料和当前版本生成、等待 Owner 编辑和批准的 Markdown 文档 |
| 当前产品方案 | 当前唯一生效、允许查询和下载的完整 Markdown 产品文档 |
| 历史产品方案 | 曾经生效、已经被新版本替代的只读 Markdown 文档 |
| Schema | 约束 AI 如何读取材料、组织 Wiki、更新文档和保留引用的本地规则 |
| 完善建议 | AI 基于授权项目的文档结构差异给出的候选补充方向 |
| Manifest | 当前项目、当前版本、文档路径和哈希的唯一生效指针 |

### 3.2 项目状态

```text
待初始化
→ 孵化中
→ 已有候选方案
→ 已有当前生效版本
```

- 新建项目完成后进入“待初始化”；
- 导入首批材料并启动 Ingest 后进入“孵化中”；
- 候选 Markdown 写入成功后进入“已有候选方案”；
- Owner 首次批准发布后进入“已有当前生效版本”。

### 3.3 文档版本状态

```text
候选草稿
→ 待Owner批准
→ 当前生效
→ 历史版本
```

任何失败状态都不得越过 Owner 批准直接进入“当前生效”。

---

## 四、本地项目库架构

### 4.1 根目录

默认项目库路径：

```text
~/Documents/产品文档孵化器项目库/
```

首次启动时 Owner 可以修改，设置后由系统保存。2.0 不直接重命名现有代码仓库目录。

### 4.2 项目库结构

```text
产品文档孵化器项目库/
├── .incubator/
│   ├── settings.json
│   └── product_incubator.db
├── LLD/
├── NEW_PRODUCT_A/
└── NEW_PRODUCT_B/
```

- `.incubator/settings.json` 保存 Owner、本地根目录和当前选中项目等轻量设置；
- `.incubator/product_incubator.db` 是中央 SQLite 状态库；
- 所有业务记录继续通过 `project_id` 隔离；
- 首期不为每个项目建设独立数据库，以控制技术成本。

### 4.3 单项目自动生成结构

```text
{项目ID}/
├── raw/
│   └── {年份}/
│       └── {来源ID}/
│           └── 原始文件
├── wiki/
│   ├── current/
│   │   └── 当前产品方案.md
│   ├── drafts/
│   │   └── {候选版本}/
│   │       └── 产品方案.md
│   ├── versions/
│   │   └── {历史版本}/
│   │       └── 产品方案.md
│   ├── topics/
│   ├── index.md
│   └── log.md
├── schema/
│   ├── AGENTS.md
│   ├── product-document-template.md
│   └── field-conventions.md
├── exports/
│   └── {项目名称}_产品方案_{版本号}.md
└── .incubator/
    ├── project.json
    ├── current-baseline.json
    └── source-index.json
```

### 4.4 各目录职责

| 目录 | 职责 | 写入权限 |
| --- | --- | --- |
| `raw/` | 保存不可变原始材料 | 系统只追加；AI 只读 |
| `wiki/drafts/` | 保存首版和增量候选产品方案 | AI 生成，Owner 可编辑 |
| `wiki/current/` | 保存唯一当前生效产品方案 | 仅发布服务原子写入 |
| `wiki/versions/` | 保存历史生效方案 | 仅发布服务追加，只读使用 |
| `wiki/topics/` | 预留 Wiki-LLM 主题页 | 首期只生成必要页面，可按排期裁减 |
| `schema/` | 保存 Wiki 规则、模板和字段约定 | 初始化服务生成，Owner 可维护 |
| `exports/` | 保存 Owner 主动下载的当前方案副本 | 导出服务写入 |
| `.incubator/` | 保存项目身份、Manifest 和来源索引 | 系统管理，不由 AI 直接修改 |

### 4.5 Wiki-LLM 对应关系

2.0 采用参考模式的三层思想：

- Raw sources：对应 `raw/`，是不可变来源；
- Wiki：对应 `wiki/`，是 AI 持续维护、Owner 审核发布的 Markdown 知识；
- Schema：对应 `schema/AGENTS.md` 等文件，规定结构和操作纪律。

产品文档孵化器在此基础上增加候选／当前／历史版本分离和人工批准，满足真实产品治理要求。

---

## 五、产品信息架构

2.0 一级入口收敛为五个：

```text
产品文档孵化器
├── 项目中心
├── 原始材料
├── 文档孵化
├── 当前产品
└── 检查与建议
```

“当前查询”合并到“当前产品”，“Diff、批准和发布”合并到“文档孵化”，“历史版本”合并到“当前产品”底部，避免建设过多一级页面。

### 5.1 项目中心

只保留：

- 新建项目；
- 进入或切换项目；
- 查看和打开本地项目路径。

项目卡片展示：

- 项目名称；
- 项目 ID；
- 当前状态；
- 当前生效版本；
- 原始材料数量；
- 待处理候选版本；
- 最近更新时间。

### 5.2 原始材料

提供：

- 上传或选择本地文件；
- 文件类型和大小校验；
- 本地复制归档；
- SHA-256 去重；
- 材料类型和适用范围确认；
- 归档路径显示；
- Ingest 触发和处理状态。

首期支持 `.md`、`.txt`、`.pdf`、`.docx`。如预计超期，可经 Owner 确认后将首期缩减为 `.md` 和 `.txt`。

### 5.3 文档孵化

提供：

- 首版候选方案生成；
- 新材料增量编译；
- 当前版与候选版 Diff；
- 引用、缺失项和占位符检查；
- Markdown 文本编辑；
- Owner 批准与原子发布。

首期只提供 Markdown 文本编辑，不建设富文本编辑器。

### 5.4 当前产品

提供：

- 当前生效完整产品方案；
- 当前项目和当前版本；
- Owner、发布时间和父版本；
- 当前规则查询；
- 引用与来源定位；
- 单 Markdown 下载；
- 轻量历史版本列表和查看。

### 5.5 检查与建议

合并两类能力：

1. 当前项目检查：规则冲突、结构缺失、引用缺失、失效内容和证据缺口；
2. 跨项目完善建议：基于 Owner 明确授权项目的文档结构，给出建议补充内容。

---

## 六、核心业务流程

### 6.1 首次设置

```text
填写Owner姓名
→ 采用默认项目库路径或选择其他本地路径
→ 创建.incubator配置和中央数据库
→ 完成路径可写性与空间检查
→ 进入项目中心
```

首次设置不创建登录账号和密码。

### 6.2 创建项目

Owner 填写：

- 项目名称；
- 项目 ID；
- 产品类型或一句话说明；
- 初始显示版本名，可为空；
- 是否允许调用外部模型。

项目 ID 只允许大写英文字母、数字、短横线和下划线，例如：

```text
LLD
CREDIT-CARD-01
SMALL_BUSINESS_LOAN
```

创建过程：

```text
校验项目ID和根目录约束
→ 在根目录内创建临时项目目录
→ 生成完整raw/wiki/schema等框架
→ 写入项目配置和数据库注册记录
→ 校验目录、配置和数据库一致性
→ 原子切换为正式项目目录
→ 项目进入“待初始化”
```

任一步失败都不得留下可见的半成品项目。

### 6.3 原始材料归档

```text
Owner选择文件
→ 校验类型与大小
→ 计算SHA-256
→ 检查当前项目内重复文件
→ 复制到raw/{年份}/{来源ID}/原文件名
→ 校验复制后哈希
→ 写入source-index.json和SQLite
→ 展示本地归档路径
→ 允许启动Ingest
```

规则：

- 归档完成后不依赖外部原文件继续存在；
- 同项目同哈希文件不重复保存；
- 不同项目可以各自归档同一文件；
- AI 不得修改、重命名或覆盖 `raw/` 文件；
- 删除和替换原始文件不属于首期功能；
- Ingest 失败时保留合法归档，但不得产生伪成功候选文档。

### 6.4 新项目首版孵化

```text
首批raw材料
→ AI读取材料和schema
→ 生成完整候选Markdown产品方案
→ 写入wiki/drafts/{候选版本}/产品方案.md
→ 展示使用材料、生成章节、证据不足和缺失章节
→ Owner编辑候选Markdown
→ 执行发布前校验
→ Owner批准
→ 发布为首个当前生效版本
```

首版没有父版本，不展示传统前后 Diff；展示材料覆盖、章节结构、缺失项和证据不足项。

### 6.5 已有项目增量孵化

```text
新raw材料＋当前生效产品方案＋schema
→ AI生成新候选Markdown
→ 展示当前版与候选版Diff
→ Owner编辑候选文档
→ 重新校验
→ Owner批准发布
→ 旧当前版进入wiki/versions/
→ 新候选版原子进入wiki/current/
```

发布前至少校验：

- 候选 Markdown 不为空；
- 文档存在一级主标题；
- 没有未解析系统占位符；
- 引用的原始文件存在且哈希正确；
- 候选、项目和版本 ID 一致；
- 当前基线在孵化期间没有发生变化；
- Manifest、数据库与磁盘状态一致。

### 6.6 当前产品查询

复用 1.x Query 能力，但查询范围必须限制为当前项目的当前生效文档和合法引用。

回答展示：

- 当前项目；
- 当前版本；
- 直接答案；
- 产品方案或原始材料引用；
- 回答时间；
- 实时或缓存状态。

项目没有生效版本时禁用查询，并提示先完成首版发布。

### 6.7 当前产品导出

Owner 在“当前产品”点击“下载当前产品方案”。

内容来源：

```text
wiki/current/当前产品方案.md
```

文件名：

```text
{项目名称}_产品方案_{显示版本名}.md
```

未设置显示版本名时使用内部版本 ID。导出要求：

- 只导出当前生效产品文档；
- 格式仅为 UTF-8 Markdown；
- 不包含引用清单、原始材料、审计记录或 ZIP；
- 浏览器下载内容必须与 `wiki/current/当前产品方案.md` 完全一致；
- 同时在 `exports/` 保存同内容副本；
- 同一版本重复导出覆盖同名副本，不产生重复文件。

导出结构参考 Owner 提供的 `LLD产品方案_724_1.md`：完整产品方案、清晰章节层级、表格、规则和版本说明均以标准 Markdown 表达。

### 6.8 AI 完善建议

新版本发布成功后自动触发一次结构比较：

```text
Owner选择允许参考的项目
→ 本地提取当前项目章节标题和覆盖摘要
→ 本地提取授权项目章节标题和覆盖摘要
→ 去除业务数值、客户信息和敏感原文
→ AI比较结构和主题覆盖差异
→ 生成建议清单
→ Owner采纳或忽略
```

每条建议包含：

- 建议增加的章节或内容类型；
- 建议理由；
- 参考项目；
- 当前项目缺少的内容；
- 证据充分度；
- 采纳或忽略状态。

约束：

- 未被本次明确勾选的项目不得参与比较；
- 只参考结构、字段类型和内容覆盖情况；
- 不复制其他项目的业务数值、客户信息或敏感原文；
- 建议不得直接修改当前生效文档；
- Owner 采纳后只生成下一版本候选修订任务；
- 建议生成失败不得影响已经完成的发布。

为控制成本，首期不建设跨项目向量检索，只比较 Markdown 标题、覆盖摘要和已有结构化字段。

---

## 七、版本与文件规则

### 7.1 内部版本号

默认内部版本 ID：

```text
{项目ID}-{YYYYMMDD}-{两位序号}
```

例如：

```text
LLD-20260812-01
LLD-20260812-02
```

Owner 可以填写业务显示版本名，例如 `724_1`。内部 ID 保持唯一，显示版本名用于界面和下载文件名。

### 7.2 当前版本唯一权威

`.incubator/current-baseline.json` 是该项目当前生效版本的唯一指针，至少包含：

- `project_id`；
- `version_id`；
- `display_version`；
- `parent_version_id`；
- 当前产品文档相对路径；
- 文档 SHA-256；
- 发布时间；
- Owner 姓名。

数据库保存索引和运行状态，但不得越过 Manifest 单独声明另一当前版本。

### 7.3 原子发布

发布顺序：

```text
获取项目发布锁
→ 校验当前Manifest和候选版本
→ 在临时目录生成新当前文档与新Manifest
→ 校验内容、路径和哈希
→ 将旧当前文档追加到versions/
→ 原子切换当前文档和Manifest
→ 提交数据库事务
→ 记录发布日志
→ 释放项目锁
```

任一步失败时：

- 旧当前版本继续生效；
- 不得出现两个当前版本；
- 不得丢失旧当前文档；
- 临时文件必须可安全清理或在下次启动时被恢复服务识别；
- AI 完善建议不进入发布事务。

### 7.4 历史版本

“当前产品”底部提供轻量历史列表：

- 版本；
- 发布时间；
- 父版本；
- 来源材料数量；
- 查看历史 Markdown；
- 查看版本 Diff。

首期不提供直接回滚。需要恢复历史内容时，Owner 将其复制为新候选版本并重新批准。

---

## 八、界面要求

### 8.1 整体原则

- 保持 1.x 白底、蓝色、简约金融科技风格；
- 顶部或侧边栏始终显示当前项目名称和项目 ID；
- 所有写入动作明确展示目标项目；
- 每页只设置一个主要动作；
- 不依赖颜色表达状态，必须配合文字；
- 错误提示说明“发生了什么、哪些内容没有改变、下一步怎么做”。

### 8.2 项目切换

项目切换后必须清空上一项目的页面临时状态，包括：

- 上传文件；
- 候选文档；
- 查询结果；
- Lint 结果；
- 缓存选择；
- 待提交的 Owner 决定。

页面切换期间显示新项目身份，避免 Owner 在错误项目中继续操作。

### 8.3 Markdown 编辑

首期使用轻量 Markdown 文本编辑区：

- 显示候选文件路径；
- 支持编辑和保存草稿；
- 保存前检查内容是否为空；
- 发布前必须重新执行校验；
- 不提供富文本、评论、修订留痕和多人光标。

---

## 九、安全、隔离与异常处理

### 9.1 项目隔离

- 所有数据库记录、服务命令、缓存键、文件路径和日志必须携带 `project_id`；
- 当前项目不得读取或发布其他项目的材料和候选文档；
- 文件路径必须解析到已配置项目库根目录以内；
- 禁止 `..`、符号链接逃逸和绝对路径注入绕过根目录；
- 跨项目建议只在单次授权清单内开放只读结构摘要；
- 未授权项目不得出现在模型输入或建议证据中。

### 9.2 原始文件安全

- 归档前后 SHA-256 必须一致；
- 原始文件只追加，不在应用内删除或替换；
- AI 不具有写入 `raw/` 的能力；
- 原始文件索引记录原文件名、原路径、归档路径、来源 ID、哈希、大小和导入时间；
- 原路径仅用于追溯，不作为后续读取依赖。

### 9.3 外部模型边界

- 每个项目独立设置是否允许外部模型调用；
- 未授权项目不得调用外部模型；
- 继续使用资料分级、脱敏、最小化片段和调用审计；
- 跨项目建议默认只传递结构摘要，不传递完整产品文档；
- 机密和严格受限资料继续按既有策略禁止外调；
- 本地保存不等于完全没有数据出域，界面和文档不得使用绝对安全表述。

### 9.4 异常处理

| 场景 | 系统行为 |
| --- | --- |
| 项目创建失败 | 不注册正式项目，不留下可见半成品目录 |
| 原始文件复制失败 | 不写入来源成功状态，不允许 Ingest |
| 归档后哈希不一致 | 隔离异常副本并报错，不进入正式 `raw/` 索引 |
| 重复文件 | 提示已归档的来源 ID 和路径，不重复保存 |
| Ingest 失败 | 保留合法原始归档，不产生伪成功候选文档 |
| Markdown 保存失败 | 保留上一个完整草稿，不覆盖为半文件 |
| 发布校验失败 | 旧当前版本继续生效，候选保持可编辑 |
| 发布中断 | 启动恢复服务核对 Manifest、磁盘和数据库，未一致前禁止新发布 |
| AI 建议失败 | 发布结果不回滚，允许 Owner 稍后手动重试 |
| 下载失败 | 不修改当前产品文档和 Manifest |
| 项目切换失败 | 保持原项目上下文，不进入半切换状态 |

---

## 十、1.x 到 2.0 的迁移

### 10.1 更名边界

- 2.0 产品名称、界面、项目库、目录说明和新文档统一使用“产品文档孵化器”；
- 1.x 的“产品智策”历史方案、冻结标签、验收报告和截图保持原名；
- 不批量修改历史证据中的项目名称；
- 现有代码仓库目录不在本期直接重命名。

### 10.2 LLD 迁移目标

LLD 作为一个普通项目进入 2.0 项目中心：

```text
产品文档孵化器项目库/
└── LLD/
    ├── raw/
    ├── wiki/
    ├── schema/
    ├── exports/
    └── .incubator/
```

迁移要求：

- 现有当前基线、产品方案和来源归档可读取；
- 现有当前版本继续有效；
- 迁移后 Manifest、数据库和文件哈希一致；
- 不移动或破坏 1.x 冻结快照和验收材料；
- 迁移可重复演练，正式执行前生成备份或快照；
- 迁移失败不得改变 1.x 可用状态。

### 10.3 兼容策略

2.0 新逻辑以项目库根目录为准。1.x 固定 `data/obsidian_vault` 和 `data/source_archive` 仅作为迁移来源，不继续作为所有 2.0 项目的共享正式路径。

---

## 十一、投入计划与范围控制

### 11.1 计划投入

| 工作包 | 交付内容 | 人日 |
| --- | --- | ---: |
| P1 多项目内核 | 项目注册、创建、切换、当前项目上下文和 `project_id` 隔离 | 2.5 |
| P2 本地文件体系 | 自动建目录、`raw/` 复制归档、哈希去重和路径索引 | 2.0 |
| P3 文档孵化 | 首版草稿、增量候选、Markdown 编辑、Diff 和 Owner 发布 | 3.0 |
| P4 当前产品与导出 | 当前文档查看、单 Markdown 下载和本地导出副本 | 0.5 |
| P5 检查与建议 | 复用 Lint、章节完整性和授权项目结构建议 | 1.5 |
| P6 界面收敛 | 五个一级入口、项目选择器、状态和异常提示 | 1.0 |
| P7 LLD 迁移 | 将现有 LLD 转为 2.0 项目结构并保持当前版本有效 | 1.0 |
| P8 测试与文档 | 关键单元测试、跨项目隔离 E2E 和操作手册 | 2.0 |
| 计划合计 |  | **13.5** |
| 风险缓冲 | 文件事务、多项目迁移或模型输出适配 | **1.5** |
| 投入上限 |  | **15.0** |

### 11.2 必须暂停确认的条件

出现以下任一条件，开发必须暂停并向 Owner 汇报：

- 累计实际投入超过对应工作包计划 20%；
- 多项目隔离需要大范围重写 Query、Lint 或发布内核；
- 模型无法稳定生成完整候选 Markdown；
- LLD 迁移需要兼容多套未识别的历史格式；
- 剩余工作预计使总投入超过 15 人日；
- 任一不可裁减能力与现有架构发生根本冲突。

### 11.3 超期时的裁减顺序

必须经 Owner 确认后，按以下顺序裁减：

1. 发布后自动生成完善建议，改为 Owner 手动点击；
2. 暂不生成 `wiki/topics/` 主题页；
3. 历史版本只查看 Markdown，取消版本 Diff；
4. “打开本地文件夹”只显示和复制完整路径；
5. 跨项目建议只比较一级、二级标题，不生成覆盖摘要；
6. 新项目首期只支持 `.md` 和 `.txt`，PDF 与 DOCX 延后。

不得用裁减以下能力换取工期：多项目建库、`raw/` 归档、候选到当前的 Owner 发布、当前 Markdown 下载。

---

## 十二、验收标准

### A01 创建新项目

步骤：

```text
填写项目名称和ID
→ 创建项目
→ 自动生成完整目录
→ 进入“待初始化”
→ 查看本地项目路径
```

通过标准：

- 所有必需目录和 Schema 文件存在；
- `project.json`、数据库和项目路径一致；
- 重复项目 ID 被阻止；
- 非法项目 ID 和越界路径被拒绝；
- 创建失败不留下正式半成品项目。

### A02 原始材料归档

步骤：

```text
导入文件
→ 复制到raw/
→ 校验SHA-256
→ 页面显示归档路径
→ 移动外部原文件
→ 再次读取归档副本
```

通过标准：

- 归档副本仍可读取；
- 归档前后哈希一致；
- AI 和页面流程不能覆盖原始文件；
- 同项目重复文件被识别且不重复保存；
- 不同项目可以独立归档同一文件。

### A03 首版文档孵化

步骤：

```text
导入首批材料
→ Ingest
→ 生成候选Markdown
→ Owner编辑
→ Owner批准
→ 建立首个当前版本
```

通过标准：

- 未批准前不存在正式当前版本；
- 候选文档标明使用材料、缺失项和证据不足项；
- 发布后当前文档、Manifest 和数据库一致；
- 首个版本没有伪造父版本。

### A04 增量版本迭代

步骤：

```text
导入新材料
→ 生成新候选
→ 查看Diff
→ Owner批准
→ 旧版进入versions/
→ 新版进入current/
```

通过标准：

- Diff 能显示关键前后变化；
- 旧版仍可读取；
- 当前版本唯一；
- 发布失败时旧版继续生效；
- 不一致状态下发布按失败关闭。

### A05 当前产品导出

步骤：

```text
打开当前产品
→ 点击下载当前产品方案
→ 获得{项目名称}_产品方案_{版本}.md
```

通过标准：

- 下载文件为 UTF-8 Markdown；
- 下载内容与 `wiki/current/当前产品方案.md` 字节一致；
- 只包含当前产品方案，不包含其他材料；
- `exports/` 同名副本内容一致；
- 无当前版本时下载按钮不可用。

### A06 AI 完善建议

步骤：

```text
发布新版本
→ 选择允许参考的项目
→ 生成结构建议
→ 采纳一条建议
```

通过标准：

- 建议标明参考项目、缺失内容、理由和证据充分度；
- 未授权项目不出现在输入和输出中；
- 输出不复制其他项目敏感原文或业务数值；
- 采纳后形成候选修订任务；
- 当前生效文档没有被直接修改；
- 建议失败不影响已发布版本。

### A07 跨项目隔离

同时创建项目 A 和 B，分别执行导入、查询、候选、发布、缓存和下载。

通过标准：

- A 页面不出现 B 的材料、候选、缓存和版本；
- B 的操作不改变 A 的文件和 Manifest；
- 项目切换会清空上一个项目的临时页面状态；
- 未授权时不能使用 B 为 A 生成结构建议；
- 路径穿越和符号链接逃逸测试被阻止。

### A08 LLD 迁移

通过标准：

- LLD 作为普通项目出现在项目中心；
- 现有产品方案、当前版本和原始材料可读取；
- 迁移后可执行查询、增量候选、发布和下载；
- 1.x 冻结快照和历史验收材料保持不变；
- 迁移失败时 1.x 状态保持可用。

### A09 重启恢复

通过标准：

- 应用重启后能恢复项目列表和当前项目；
- 每个项目的当前版本与 Manifest 一致；
- 未完成临时发布被识别并阻止继续发布；
- 完整候选草稿不会因重启丢失。

### A10 投入与范围

通过标准：

- 实际投入不超过 15 人日；
- 未引入登录、多角色、富文本、复杂图谱和多格式导出；
- 任何范围裁减都有 Owner 确认记录；
- 四项不可裁减能力全部通过验收。

---

## 十三、分批交付与确认点

| 批次 | 交付节点 | 主要验收 | 计划人日 | 确认点 |
| --- | --- | --- | ---: | --- |
| B1 | 多项目内核与自动建库 | A01、基础隔离 | 2.5 | 确认新项目目录与切换体验 |
| B2 | 原始材料本地归档 | A02、路径安全 | 2.0 | 确认 `raw/` 和本地定位 |
| B3 | 首版与增量文档孵化 | A03、候选编辑、Diff | 3.0 | 确认候选文档质量与编辑流程 |
| B4 | Owner 批准与原子发布 | A04、重启恢复 | 1.5 | 确认当前／历史版本切换 |
| B5 | 当前产品下载与 AI 建议 | A05、A06 | 2.0 | 确认 Markdown 文件与建议边界 |
| B6 | LLD 迁移、全链验收和文档 | A07–A10 | 2.5 | 确认 2.0 首期是否正式通过 |
| 计划合计 |  |  | **13.5** | 另留 1.5 人日风险缓冲 |

每批完成后暂停，由 Owner 确认完成内容、下一批范围和累计人日，再继续开发。

---

## 十四、2.0 首期完成定义

只有以下条件全部成立，才能宣布产品文档孵化器 2.0 首期完成：

- 五个一级入口可用；
- 至少两个项目能独立完成关键流程；
- 每个项目都有独立 `raw/wiki/schema`；
- 首版与增量版本均能完成 Owner 批准发布；
- 当前产品文档能单独下载为 Markdown；
- AI 能基于授权项目给出结构完善建议；
- 跨项目隔离、路径安全、原子发布和恢复测试通过；
- LLD 已作为普通项目完成迁移验证；
- 1.x 历史材料未被改写；
- 实际投入不超过 15 人日；
- 所有范围裁减均经过 Owner 确认。

2.0 完成后，下一阶段再评估登录协作、富文本、多格式导出、文件监听、完整 Wiki 主题网络和企业系统集成，不提前进入本期开发。

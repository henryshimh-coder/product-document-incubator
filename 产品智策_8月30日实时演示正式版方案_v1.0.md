# 产品智策：8 月 30 日实时演示正式版方案

## 版本信息

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 编制日期 | 2026 年 7 月 29 日 |
| 正式版就绪日 | 2026 年 8 月 30 日 |
| 使用场景 | 2026 年 9 月比赛现场实时汇报 |
| 前置版本 | `产品智策_8月24日轻量交付版方案_v1.0.md` |
| 上位方案 | `产品智策_LLM-Wiki项目计划方案框架_v0.4.md` |
| 汇报口径 | `产品智策_获奖导向汇报方案_v1.0.md` |
| 总体流程图 | `产品智策_路线A_总体流程图.drawio` |

---

## 一、正式版定义

### 1.1 本版定位

8 月 30 日正式版不是另起一套系统，而是在 8 月 24 日轻量交付版基础上的演示强化版本。

强化目标只有四个：

1. **实时**：现场正常路径真实调用模型并生成本次结果；
2. **完整**：从当前基线到新版本发布的流程不跳步；
3. **稳定**：网络、模型或环境异常时可平滑降级；
4. **可信**：所有核心结论有引用、版本、状态和实测证据。

### 1.2 正式版成功标准

9 月现场能够在 5–8 分钟内实时完成：

> 打开当前产品 → 导入一份新资料 → AI 实时编译 → 查询当前规则 → 实时发现冲突 → 人工决策 → 生成变更单 → 批准发布新版本 → 展示完整追溯和实测价值。

同时满足：

- 正常路径不依赖事先写死的展示文本；
- 实时结果保留本次模型调用编号和时间；
- 降级结果显著标记，不能冒充实时结果；
- 演示失败不会破坏产品基线和正式材料；
- 现场重置后可以重新开始；
- PPT、讲解、系统数据和答辩口径完全一致。

---

## 二、从 8 月 24 日版升级什么

### 2.1 保持不变

- 六个工作区；
- 核心业务对象和状态；
- 当前生效优先原则；
- Baseline Manifest 唯一权威；
- 人工批准发布；
- 正式与模拟材料边界；
- 市场证据缺口代表性能力；
- 轻量成本联动；
- 不建设完整市场、损益、会计和多角色系统的范围约束。

### 2.2 必须强化

| 强化项 | 8 月 24 日状态 | 8 月 30 日正式版要求 |
|---|---|---|
| 模型调用 | 实时可用＋缓存兜底 | 正常路径现场实时调用 |
| 演示材料 | 可完成端到端 | 压缩为适合现场的脱敏短材料 |
| 输出速度 | 功能可用 | 满足演示时间预算 |
| 结果质量 | 第一轮黄金测试 | 完成回归、引用核验和误报收口 |
| 界面 | 六区可操作 | 状态清楚、操作聚焦、等待可感知 |
| 演示重置 | 有说明或工具 | 一键或单步骤恢复初始基线 |
| 故障处理 | 缓存可接管 | 实时、缓存、视频三层切换 |
| 价值证明 | 初步记录 | 形成可上 PPT 的复核结果 |
| 讲解 | 初版脚本 | 系统操作和口播逐秒对齐 |

### 2.3 不允许借正式版扩张

8 月 25–30 日不新增：

- 新一级页面；
- 新材料大类；
- 新核心状态；
- 新专业 Agent；
- 完整市场和财务模块；
- 多角色登录；
- 生产系统接口；
- 自动授信或风险决策；
- 与主故事无关的图谱节点和动画。

---

## 三、正式现场故事

### 3.1 故事选择原则

正式演示只讲一个跨资料、可决策、可发布的产品冲突。

演示问题必须满足：

1. 普通评委能够在 20 秒内听懂；
2. 同时涉及当前产品和至少一份新材料；
3. 有明确原文依据；
4. 能产生可解释的产品影响；
5. 能通过会议选择形成变更单；
6. 发布后可以直观看到修改前后差异；
7. 不暴露公司机密或客户敏感信息。

首选问题类型：

1. 客群准入或排除边界；
2. 额度、期限或产品流程；
3. 推荐奖励规则及轻量成本影响；
4. 产品规则与技术实现版本不一致。

最终问题从已复核黄金测试集中选择，不在方案中写死真实业务数值。

### 3.2 正式材料集

现场只使用四份材料中的必要部分：

| 材料 | 现场角色 |
|---|---|
| 当前产品方案 | 初始生效基线 |
| 风险意见 | 新导入材料或冲突依据 |
| 会议纪要 | 权威识别和决议状态展示 |
| 技术方案／需求评审 | 跨版本一致性依据 |

现场实时上传材料应：

- 已完成脱敏和安全复核；
- 页数和文本量适合 60 秒内处理；
- 保留真实结构和代表性内容；
- 具有明确文件版本、日期和权威；
- 文件哈希在演示材料清单中登记；
- 同时准备一份完全相同的缓存结果。

---

## 四、正式演示流程

### 4.1 7 分钟标准脚本

| 时间 | 页面和操作 | 评委应理解的结论 |
|---|---|---|
| 0:00–0:30 | 项目首页，展示当前基线 | 系统管理的是“当前产品”，不是文件堆 |
| 0:30–1:20 | 上传新资料并确认权威、安全标签 | AI 输入是受控的，不是随意上传 |
| 1:20–2:10 | 实时 Ingest，展示编译结果 | 新资料被转成证据、建议、冲突和关系 |
| 2:10–2:50 | 一键查询当前规则 | 回答区分当前、候选和历史，并有引用 |
| 2:50–4:05 | 一键 Lint，打开代表性问题卡 | AI 主动发现跨资料和跨版本矛盾 |
| 4:05–5:10 | 会议决策并生成变更单 | AI 给依据，人作决定 |
| 5:10–6:00 | 人工批准并发布新基线 | 产品像代码一样受控发布和保留历史 |
| 6:00–6:40 | 展示关系追溯、市场缺口和成本联动 | 变化影响可追溯、可解释 |
| 6:40–7:00 | 展示实测结果和二期路线 | 项目已验证，且具备推广边界 |

现场获得更长时间时，增加 Query 问题或引用展开；时间缩短时，优先压缩首页和价值页，不删除人工决策与版本发布。

### 4.2 不可删除的三个画面

1. 冲突双方的原文引用；
2. 变更前后和人工批准动作；
3. 新基线、父版本和完整追溯链。

这三个画面共同证明项目不是普通 RAG 问答。

### 4.3 现场操作原则

- 每一页只保留一个主按钮；
- 关键动作前显示状态和影响；
- 实时等待时显示正在执行的步骤；
- 不在现场输入复杂 Prompt；
- 不临时修改模型、Prompt、Schema 或材料；
- 不临时联网搜索市场事实；
- 不演示与主故事无关的功能；
- 不在现场直接打开包含敏感内容的原始全文。

---

## 五、六个工作区的正式版要求

### 5.1 项目首页与当前基线

正式版必须做到：

- 3 秒内看懂当前产品版本；
- 明确显示“当前生效”状态；
- 显示开放问题、候选变更和最近导入；
- 支持一键恢复演示初始状态；
- 进入主流程只需一次点击；
- 不使用复杂图表抢夺注意力。

### 5.2 资料导入

正式版必须做到：

- 自动预识别材料类型、权威和安全等级；
- 人工通过单选、下拉和复选完成确认；
- 显示本次是否允许外部模型调用；
- 显示材料文件名、哈希摘要和适用版本；
- 实时展示“本地处理—模型分析—结构校验—写入完成”；
- 失败时明确给出“重试实时调用”或“使用缓存结果”。

### 5.3 当前产品查询

正式版必须做到：

- 默认只查询当前生效版本；
- 直接答案优先，不先展示长篇推理；
- 引用默认收起、点击展开；
- 明确提示候选变化和开放冲突；
- 显示产品版本和回答时间；
- 证据不足时直接说明缺失材料。

### 5.4 一键自检与会议决策

正式版必须做到：

- 默认只展示 2–3 个最具代表性的问题；
- 每个重大问题有冲突双方引用；
- 显示问题等级、影响范围和 AI 推荐的不确定性；
- 四种会议操作清晰可选；
- 接受迭代后立即形成正式决策记录；
- 判定误报后保留记录，不直接删除。

### 5.5 变更发布

正式版必须做到：

- 一屏展示修改前、修改后、依据和影响；
- 明确显示“当前演示确认人”和“正式应批准角色”；
- 人工批准按钮与普通操作视觉区分；
- 发布前完成版本一致性校验；
- 发布成功后显示新版本号和父版本；
- 发布失败时显示旧版仍然生效。

### 5.6 追溯与价值证明

正式版必须做到：

- 关系图只显示主故事相关节点；
- 点击节点可展开来源和状态；
- 展示一条市场证据缺口；
- 展示一条轻量成本联动；
- 展示实际人工对照结果；
- 展示一次模型调用审计记录；
- 二期路线不超过一屏。

---

## 六、实时技术与性能设计

### 6.1 实时调用识别

每次现场实时调用必须记录：

- 调用编号；
- 任务类型；
- 模型名称；
- Prompt 版本；
- 来源文件编号；
- 请求开始和结束时间；
- 是否实时结果；
- 是否使用缓存；
- 结构校验结果；
- 写入结果。

前端显示：

```text
本次结果：实时生成
调用时间：现场时间
调用编号：可展开查看
```

使用缓存时显示：

```text
本次结果：演示缓存
缓存对应材料：当前文件哈希
缓存生成时间：原始生成时间
```

### 6.2 演示时间预算

| 操作 | 目标时间 | 超时后的处理 |
|---|---:|---|
| 打开项目与基线 | 3 秒内 | 读取本地快照 |
| 上传和本地预处理 | 10 秒内 | 缩小实时材料范围 |
| Ingest 模型分析 | 45 秒内 | 60 秒提示切换缓存 |
| Query | 20 秒内 | 30 秒提示切换缓存 |
| Lint | 45 秒内 | 60 秒提示切换缓存 |
| 会议决策和变更单 | 5 秒内 | 使用本地确定性流程 |
| 发布新基线 | 5 秒内 | 失败则保持旧版并显示原因 |
| 关系追溯 | 3 秒内 | 读取本地关系快照 |
| 演示重置 | 30 秒内 | 恢复冻结的初始快照 |

目标时间用于控制演示材料和工作流，不作为模型供应商服务承诺。

### 6.3 输出结构校验

实时模型输出进入系统前必须通过：

- JSON 可解析；
- 必填字段齐全；
- 状态枚举合法；
- 来源引用存在；
- 版本字段合法；
- 不允许直接输出 `effective`；
- 不允许修改原始资料；
- 结论和引用范围一致；
- 重复对象按编号或内容指纹合并。

结构校验失败时：

1. 自动重试一次；
2. 第二次失败后提示用户；
3. 用户可选择使用同材料缓存；
4. 失败内容不写入正式知识区。

### 6.4 缓存键

缓存必须至少由以下信息共同确定：

```text
文件哈希
+ 任务类型
+ 产品基线版本
+ Prompt 版本
+ 模型标识
+ Schema 版本
```

任一关键项变化，不得静默复用旧缓存。

---

## 七、现场三层保障

### 7.1 第一层：实时主流程

适用条件：

- 网络正常；
- 外部模型可用；
- 授权和脱敏检查通过；
- 结构化输出校验通过。

系统按正常路径完成全流程，并记录本次实时调用。

### 7.2 第二层：同材料缓存

触发条件：

- 外部模型超时；
- API 限流；
- 模型结构化输出连续两次不合法；
- 现场时间不允许继续等待。

要求：

- 缓存与当前文件哈希一致；
- 页面显著显示“缓存结果”；
- 后续 Query、决策和发布仍真实执行；
- 讲解主动说明模型异常已被工程化降级；
- 不把缓存描述为现场实时生成。

### 7.3 第三层：备用视频

触发条件：

- 应用无法启动；
- 本地环境损坏；
- 页面显示或投屏完全异常；
- 无法在 60 秒内恢复。

备用视频必须：

- 使用正式版同一界面、材料和版本；
- 时长不超过正式脚本；
- 展示完整人工决策和版本发布；
- 标明录制日期和应用版本；
- 可本地离线播放。

---

## 八、正式版评测

### 8.1 Query

- 10 个黄金问题；
- 目标准确率不低于 90%；
- 当前、候选和历史状态区分正确率为 100%；
- 重大回答引用覆盖率为 100%；
- 引用必须能够支持答案，不以“存在引用”代替“引用有效”。

### 8.2 Lint

- 8–10 个黄金问题；
- 目标识别率不低于 80%；
- 所有重大问题均有双方引用；
- 记录误报、漏报和人工结论；
- 至少包含两个不应判为冲突的版本／适用范围差异。

### 8.3 版本和发布

- 未批准变更不能发布；
- 发布成功后 Manifest、Markdown 和 SQLite 一致；
- 旧版本可以查询；
- 发布失败不影响旧版；
- 重复发布同一变更必须阻断或明确提示。

### 8.4 安全

- 未授权文件外调阻断率 100%；
- L3／L4 资料外调阻断率 100%；
- 每次模型调用有日志；
- 单文件累计出域比例可查询；
- 正式和模拟材料不混淆。

### 8.5 人工对照

使用相同材料分别完成人工和系统任务：

1. 查找当前规则；
2. 识别跨文档冲突；
3. 整理会议议题；
4. 形成产品变更单；
5. 追溯规则来源。

只发布经过复核的实际数据。若某项结果没有形成可靠记录，汇报材料不展示该数字。

---

## 九、8 月 25—30 日强化计划

### 8 月 25 日：正式版基线和故事冻结

- 从黄金测试集中选择唯一主故事；
- 冻结现场材料和文件哈希；
- 冻结 7 分钟脚本；
- 冻结页面顺序；
- 建立正式版分支或快照；
- 建立阻断问题清单。

### 8 月 26 日：实时链路和时间优化

- 测量 Ingest、Query、Lint 的实际耗时；
- 压缩现场材料；
- 优化 Prompt 和结构输出；
- 校验实时调用日志；
- 完成超时和重试策略；
- 保证缓存键正确。

### 8 月 27 日：正式 UI 和证据

- 收口每页主操作；
- 优化状态、版本和引用显示；
- 固化关系追溯视图；
- 完成人工对照数据复核；
- 完成市场缺口和成本联动展示；
- 形成 PPT 可用截图。

### 8 月 28 日：故障演练

- 模拟断网；
- 模拟 API 超时和限流；
- 模拟不合法 JSON；
- 模拟发布失败；
- 验证缓存和旧版保护；
- 录制备用视频初版。

### 8 月 29 日：全流程彩排

- 在正式演示设备运行；
- 在投屏环境运行；
- 完成不少于 5 次全流程彩排；
- 记录每次时长、错误和恢复方式；
- 修复阻断问题；
- 完成备用视频终版。

### 8 月 30 日：正式冻结

- 完成累计不少于 10 次全流程演练；
- 最后连续 3 次无阻断完成；
- 完成实时、缓存和视频三种模式验证；
- 冻结代码、配置、模型、Prompt、数据和缓存；
- 冻结演示设备环境；
- 冻结 PPT、脚本、视频和答辩材料；
- 生成正式版版本清单和哈希清单。

---

## 十、正式版交付物

### 10.1 系统包

- 正式版应用；
- 固定依赖和启动配置；
- Dify 工作流导出；
- 模型配置说明；
- 初始演示快照；
- 演示重置工具；
- 实时／缓存切换机制；
- 本地启动和故障处理手册。

### 10.2 数据包

- 正式脱敏演示材料；
- 初始产品基线；
- 黄金测试集；
- 缓存结果；
- 模型调用日志样例；
- 人工对照记录；
- 正式／模拟材料清单；
- 版本和文件哈希清单。

### 10.3 汇报包

- 正式 PPT；
- 7 分钟讲解脚本；
- 页面操作清单；
- 备用视频；
- 答辩问题库；
- 一页纸项目摘要；
- 生产推广路线图；
- 现场设备检查表。

---

## 十一、正式版 Go／No-Go 门禁

8 月 30 日只有同时满足以下条件，才能标记“正式演示版就绪”：

1. 唯一主故事已冻结；
2. 正式演示材料通过脱敏和授权复核；
3. 实时路径完整跑通；
4. 缓存路径完整跑通；
5. 备用视频可以离线播放；
6. Query 和 Lint 达到目标；
7. 重大结论引用覆盖率为 100%；
8. 未批准变更不能发布；
9. 发布失败不破坏当前基线；
10. 最近连续 3 次全流程无阻断；
11. PPT、系统、视频和答辩使用同一版本、名称和数据；
12. 不存在未关闭的演示阻断问题或安全阻断问题。

任一条件不满足时，正式版状态为“限条件演示”，必须在现场方案中明确采用缓存或视频路径，不得宣称已经完全就绪。

---

## 十二、9 月管理规则

8 月 30 日冻结后：

- 不新增功能；
- 不更换主故事；
- 不更换核心模型，除非原模型不可用；
- 不修改 Schema 和状态；
- 不替换已复核的正式材料；
- 不临时增加市场和财务数字；
- 只允许修复阻断缺陷、错别字、显示问题和设备适配；
- 每次修改必须重新完成至少一次完整彩排；
- 现场前一天只做环境检查，不做功能开发。
